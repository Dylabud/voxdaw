# VoxDaw - Project Plan & Execution Log

## How to Update This File
* **For Dylan:** Add broad milestones or specific features to the "Future Steps" section as the project vision evolves.
* **For Claude:** When you complete a step or milestone during a session, you must cut that item from "Future Steps" and paste it into the "Completed Steps Log." Add a brief bullet point explaining the technical implementation (e.g., libraries used, file names created). Do not overwrite history.

---

## Status: Work In Progress
VoxDaw is actively under development. Core gesture engine, audio DSP chain, arpeggiator, piano roll, loop station, and MIDI output are complete. Remaining work is focused on visual polish and further UX refinement.

---

## Future Steps

### Visual Polish
* [ ] Apply a stylized filter (dot-matrix/silhouette) to the live camera feed in the main UI viewport.
* [ ] Optimize for latency and ensure the React component tree is not unnecessarily re-rendering.

### Known Issues
* [ ] **Right-wall scroll-past stopper still occurs intermittently** after Phases 92/93. Phases 92/93 closed the marquee-stale-`left` and piano-roll-grid-flex-stretch paths but a third path remains unidentified — repro is non-deterministic. Next investigation: log `scrollWidth` vs `totalMeasures * pixelsPerMeasure` on the bug and walk child `getBoundingClientRect().right` values to find which element is still extending the scroll container.

---

## Completed Steps Log

* **[2026-05-20] Phases 89–93 — Region Creation Slicing, Zoom Cursor-Anchor Unification & Void-Past-200 Fixes:**

  * **Phase 93 — Piano-roll grid void past 200 measures (`RegionEditor.jsx` + `.module.css`):** With editor open, the user could glitch a "void" of empty gridlines past measure 200 via `scroll right → zoom in → scroll right → zoom out with cursor on left`. Root cause: `.grid` was `flex: 1` with `minWidth: gridMinWidth`. At extreme zoom-out (or under mid-zoom transient layout), the flex algorithm stretched `.grid` past `totalMeasures * pixelsPerMeasure` to fill `.pianoRoll`'s available space, while the `repeating-linear-gradient` background tiled infinitely. Fix: replaced `minWidth: gridMinWidth` with `width: gridMinWidth` in the inline style, and changed `.grid`'s `flex: 1` to `flex: 0 0 auto`. The grid is now sized exactly like `.timelineInner` (line 1232) — explicit width, no stretch. Any leftover horizontal space inside `.pianoRoll` shows the scroll container's own background, identical to how `.timeline` shows `--bg-base` past the inner div. Gemini's notes (re-proposing the already-shipped Phase 85/86 auto-scroll + live marquee preview) were entirely unrelated to the bug — rejected and the real root cause traced from CSS layout.

  * **Phase 92 — Marquee stale `style.left` extending timeline scrollWidth (`WorkstationShell.jsx:740`):** After marquee-dragging against the right wall (auto-scroll pins the box at max scrollLeft), the absolutely-positioned `marqueeElRef` div retained its inline `left` (e.g. 19000px) on `onUp` — only `width`/`height`/`opacity` were cleared. An abs-positioned child whose right edge exceeds its containing block extends the ancestor scroll container's `scrollWidth` in modern browsers, so even after a zoom-out shrunk `timelineInner.width`, the user could scroll into the now-uncovered range (void past 200 measures). Fix: also clear `el.style.left = ''` and `el.style.top = ''` alongside the existing resets. Single-line addition.

  * **Phase 91 — Editor open jumps timeline scrollLeft to 0 (`WorkstationShell.jsx`):** When the user clicked "edit" with a scrolled timeline, the arrangement jumped to leftmost. Trace: `pianoScrollRef.current` mounted fresh with `scrollLeft=0`; `RegionEditor`'s mount `useEffect([track?.id])` set `container.scrollTop`, which fired a scroll event whose `scrollLeft=0` was then propagated by `handlePianoRollScroll`'s value-comparison guard (0 ≠ current timeline scrollLeft → write succeeds → timeline jumps). Fix: added a `useLayoutEffect([editingTrackId])` in WorkstationShell that primes `pianoScrollRef.current.scrollLeft = timelineRef.current.scrollLeft` when `editingTrackId` becomes truthy. Parent `useLayoutEffect` fires after child `useLayoutEffect` but before any `useEffect`, so it lands before RegionEditor's mount effect — when the subsequent scroll event fires, scrollLeft already matches and the value-comparison guard short-circuits.

  * **Phase 90 — Zoom Cursor-Anchor Unification + Sensitivity + Ruler Drag Auto-Scroll (`WorkstationShell.jsx`):** Five sub-fixes. (1) **Zoom sensitivity:** wheel factors rolled back from Phase 84's `0.95/1.05` to `0.9/1.1` (matches industry DAW feel). (2) **Ruler drag auto-scroll:** `handleMouseDown` ruler branch now seeds `currentMousePosRef` and calls `startAutoScroll()`; the window `mousemove`/`mouseup` `useEffect` updates `currentMousePosRef` each frame and calls `stopAutoScroll()` on release — reuses the Phase 85 engine. (3+4) **Cursor-anchored zoom + jump-on-zoom-far-right (root cause traced):** Both wheel handlers (timeline + piano roll) were writing the source panel's `pendingScrollRef` as cursor-anchored but the *counterpart*'s as `counterpart.scrollLeft * (next/prev)` (proportional/left-edge scaling). After `useLayoutEffect` applied both, the two values diverged; the scroll-sync handlers raced; whichever fired last clobbered the other panel's cursor-anchored value with the divergent proportional one — producing the visible "grid jumps left/right" on zoom (worse the farther right because divergence is linear with `scrollLeft * (1 − next/prev)`). Fix: in both handlers, set both pendings to the SAME cursor-anchored value (the cursor anchors a single measure; both panels' scrollLeft must land where that measure sits at the cursor's content X). Critique of Gemini's plan: misdiagnosed as "browser scroll-clamps because the container hasn't expanded yet" and proposed direct DOM-width pre-expansion via `gridContentRef.current.style.width`. That's redundant — React's commit phase writes `style.width` before `useLayoutEffect` runs, before any paint. Gemini's "React state being used for scroll sync" also wrong — Phase 88 already mutates DOM directly. (5) **Bug 4 (1-frame chase) deferred per user choice** to preserve native trackpad inertia; structural latency only removable via wheel interception + preventDefault (Gemini's Part 3) which costs inertia.

  * **Phase 89 — Region Creation Through Destructive Slicer (`WorkstationShell.jsx`):** On a track that already contained a region, double-clicking empty grid would spawn a 1-measure region that visually stacked over a fractional-start neighbor (repro: existing region at startMeasure=1.5, dur=1 → double-click at measure=1 → new [1,2) overlapped [1.5,2) because `isPositionOccupied(measure=1)` only checked the start point). Fix: lane `onDoubleClick` (line 1267) now routes the new region through `applyDestructiveEdit` like drag-`onUp` and copy/paste do — overlapping neighbors get left-trimmed / right-trimmed / split / eclipsed per Case A/B/C/D; the cull pattern from Phase 76 (`durationMeasures > 0` + orphan-note filter) is reused. Same wrap applied to `handleNoteAdd` (line 204) auto-create branch for consistency. Single source of truth for region collision resolution preserved. Critique of Gemini's plan: structurally correct, but (a) Gemini implied multiple creation sites needed the fix when only `onDoubleClick` was the offender (single-click `handleLaneClick:262` early-returns when any region exists on the track); (b) Gemini's "Part 2 / zero-culling" was already implemented at line 593–595; (c) Gemini's "partition into sameTrackRegions vs otherRegions" was unnecessary because `applyDestructiveEdit` already filters by `trackId` internally (line 289).

* **[2026-05-20] Phases 82–88 — Workstation Snap, Auto-Scroll, Scroll Sync & Grid Scaling:**

  * **Phase 88 — Scroll Sync Fix & Power-of-2 Grid Scaling:** Three bugs fixed. (1) **Horizontal/vertical scroll sync latency:** The `syncingScrollRef` / `syncingVerticalRef` boolean guards were synchronously reset to `false` within the same JS stack frame that set them — by the time the browser asynchronously dispatched the triggered `scroll` event on the counterpart element, the guard was already cleared, allowing a retaliatory handler re-run that could produce 1-frame visual lag. Fix: replaced boolean-flag guards with **value-comparison guards** in all three handlers (`handleTimelineScroll`, `handlePianoRollScroll`, `handleTrackHeadersScroll`). Each handler only writes `scrollLeft`/`scrollTop` to the counterpart if the value would actually change — if equal, no DOM write, no `scroll` event fires, no handler re-runs. `syncingScrollRef` and `syncingVerticalRef` removed entirely. Gemini misidentified the root cause as "React state being used for scroll sync" — the implementation already used direct DOM mutation; the actual bug was in the guard timing. (2) **Non-musical grid intervals:** `Math.ceil(50 / ppm)` produced arbitrary non-musical intervals (3, 5, 6, 7…). Fix: added `getMeasureInterval(ppm)` module-level helper (`Math.pow(2, Math.max(0, Math.ceil(Math.log2(80 / ppm))))`) that always returns a power of 2 (1, 2, 4, 8, 16). Applied to both `computeGridBg` (major gridline period) and the ruler label rendering loop. `getSnapIncrement` is threshold-based (ppm ≥ 150/300) and was not affected.

  * **Phase 87 — Global Snap Toggle (Magnet) & Playhead Snapping:** Added a magnet toggle button (`⌘`) in the `.transportLeft` transport bar. Default ON. **State + ref:** `[snapEnabled, setSnapEnabled] = useState(true)` + `snapEnabledRef = useRef(true)` inline-synced each render. **`MIN_REGION = 0.0625`** module-level constant (one 16th note in measures) — hard minimum region width when snap is OFF, replacing `snapInc` as the resize clamp floor. **Drag `onMove`:** all three modes read `snapEnabledRef.current`; OFF passes raw pixel delta directly. Companion clamping uses `snapEnabledRef.current ? snapInc : MIN_REGION`. **`seekToClientX`:** snaps playhead to finest visible subdivision when ON, else continuous. Deps array expanded to `[pxPerSec, updatePlayhead, snapEnabled, pixelsPerMeasure, bpm]`. Button reuses existing `transportBtn` / `transportBtnActive` CSS.

  * **Phase 86 — Auto-Scroll Integration Bug Fixes (5 bugs):** (1) Marquee live preview stale during scroll — `currentMousePosRef` and `startAutoScroll()` were missing from the marquee `onMove` path; added both. (2) Synthetic `mousemove` missing `buttons: 1` — drag handlers that check `e.buttons` skipped the synthetic event; added `buttons: 1` to the `MouseEvent` constructor. (3) Region X stale during auto-scroll — `rawDelta` was pure client-space; fixed by storing `startScrollLeft` in `dragRef.current` and adding `scrollAdjust = scrollLeft - startScrollLeft`. (4) Marquee X stale during auto-scroll — same scroll-adjust pattern applied. (5) Auto-scroll stopped at boundary — original `> 0` guard prevented scrolling when mouse exited the timeline; fixed with `Math.min(1, 1 - dist / THRESHOLD)` cap.

  * **Phase 85 — Edge Auto-Scroll Engine & Live Marquee Preview:** (1) **Auto-scroll engine:** `startAutoScroll` / `stopAutoScroll` (`useCallback([], [])`) manage a rAF loop reading `currentMousePosRef` and `timelineRef.current.getBoundingClientRect()` each tick. 80px edge threshold, 15px/frame max speed. After any scroll delta, a synthetic `mousemove` (`buttons: 1, bubbles: true`) is dispatched so all existing drag/marquee handlers re-fire with updated scroll state — no position logic duplicated in the tick. `stopAutoScroll` called on `mouseup` in both drag and marquee effects. (2) **Live marquee region preview:** Each marquee `onMove` frame calls `getIntersectingRegionIds(...)` with the current box; previously-highlighted regions have `filter` removed; intersecting regions get `filter: brightness(0.6)` via `document.querySelector('[data-region-id]')`. `marqueeHoverIdsRef = useRef(new Set())` tracks the active set; highlights cleared on `mouseup` before `setSelectedRegionIds`. Gemini proposed `setPendingMarqueeIds` React state (60fps re-renders) — replaced with DOM-direct approach to honour the Zero-Re-render Rule.

  * **Phase 84 — UI Polish: Ghost Suppression, Zoom Ghost Desync, Zoom Sensitivity:** (1) `handleLaneMouseMove` returns early when `marqueeDragRef.current` is set — prevents ghost regions appearing during a marquee drag. (2) Both `onWheel` handlers now clear all ghost opacities before `setZoomLevel` — without this, ghost divs remained at stale `translateX` positions after zoom rescaled `pixelsPerMeasure`. (3) Wheel zoom factors changed from `0.9/1.1` → `0.95/1.05` — half the step per tick, matching industry-standard DAW zoom feel.

  * **Phase 83 — Absolute Coordinate Snapping (Fix Off-Grid Offset Preservation):** Phase 82's delta-snapping preserved fractional offsets: a region at measure 0.25 dragged by 1.0 would land at 1.25 instead of 1.0. Fix: switched all three modes to **absolute coordinate snapping** — snap the raw destination coordinate (`initStart + rawDelta`) to the nearest subdivision, not the delta. Move formula: `snappedStart = Math.round((initStart + rawDelta) / snapInc) * snapInc; actualDelta = snappedStart - initStart`. Same pattern for resize-right and resize-left. Regions always land on clean grid positions regardless of where within a measure the drag started.

  * **Phase 82 — Dynamic Subdivision Grid Snapping:** Region drag/resize now snaps to the finest visible grid subdivision. Added `getSnapIncrement(ppm)` module-level helper: `ppm >= 300 → 0.125` (8th notes), `ppm >= 150 → 0.25` (quarter notes), `else → 1` (measures). Thresholds match `computeGridBg` exactly — beat lines appear at `zoomLevel >= 1.5` (`ppm >= 150`), 8th-note lines at `zoomLevel >= 3.0` (`ppm >= 300`). `snapInc` computed once per `onMove` invocation and applied across all three drag modes. Gemini proposed thresholds `ppm >= 160/80/40` which were out-of-sync with the actual drawn grid lines — corrected.

* **[2026-05-19] Phase 81 — Cluster Sort Tie-Break Through Wall-Clamp (resize-left eclipse fix):**
  User reported: two regions on the same track both selected, A at startMeasure=0 (touching the left wall) and B to the right. Dragging B's left handle aggressively leftward to fully eclipse A produced a near-revert on drop — B gained almost no leftward expansion and A survived. Traced the bug to Phase 75's cluster sort using `pendingStart` as the secondary key. When the wall-clamp pins both members to `pendingStart=0`, the sort sees a tie and JavaScript's stable sort falls back to the cluster-build insertion order `[movedRegion=B, ...companions=A]` → B processed first into resolvedCluster, A incoming → A's window `[0,4)` lies inside B's window `[0,~10)` → Case C left-trim of B, A wins. The intended Case A (B eclipses A) never fires because B was added to resolvedCluster before A could trigger it as the incoming. **Critique of Gemini's plan**: Gemini diagnosed this as a "silent revert" caused by an early `return` in `onUp` and proposed hunting for bounds-clamp aborts plus auditing Case A. Both wrong — no early return exists, Case A is implemented correctly; the bug is that Case A is never reached because the sort puts the wrong member as incoming. **Fix**: Added an `initStartById` Map (sidecar to keep cluster objects clean) populated from `d.initStart` and each companion's `c.initStart` in `onUp`, then changed the sort comparator to use `initStartById.get(a.id) - initStartById.get(b.id)` instead of `a.startMeasure - b.startMeasure`. Pre-drag startMeasures don't collapse under wall-clamping, so the intrusion-direction signal survives — B (initStart=5) sorts after A (initStart=0) under ascending order for resize-left → B is processed last → Case A fires → A is eclipsed. Phase 75 invariants for non-wall-clamped resizes and resize-right are preserved because `initStart` and `pendingStart` produce identical sort orders for uniform-delta cluster resizes outside the wall-clamp edge case.

* **[2026-05-19] Phase 80 — Void-Click Deselection:**
  Clicking in the empty area below all track lanes now clears region selection (standard DAW behavior). Added a single `else` branch to the marquee `useEffect`'s `onUp` handler in `WorkstationShell.jsx`: when `marqueeDragRef.current` exists but `d.active` is false (mousedown armed marquee but no drag occurred past the 4px threshold), call `setSelectedRegionIds(new Set())`. This catches pure void clicks cleanly because (a) `.trackLane` `onMouseDown` calls `stopPropagation` so lane clicks never reach the marquee path, (b) ruler clicks route through `isDraggingRef` in `handleMouseDown` and never set `marqueeDragRef`, and (c) successful marquee drags hit the `if (d.active)` branch and replace selection with the hits. Rejected Gemini's "clear at mousedown" approach because it would either (i) also clear on ruler scrub (breaks DAW convention) or (ii) flicker the selection during a successful marquee drag (clear → fill across two renders).

* **[2026-05-19] Phase 79 — Silence the `[region-dom-desync]` Warn (Microtask Becomes Intentional Workaround):**
  Confirmed via user repro that the Phase 77 microtask backstop is catching a real React reconciler bailout: when a region's commit-time value equals its pre-drag value (e.g. a companion trimmed back to its original 1-measure width by cluster resolution), React's reconciler diffs its own memo (old value) against the new JSX (same value), sees no change, and skips emitting a DOM write — but the DOM was mutated to a different value during drag. The microtask correctly resyncs the DOM. Removed the `console.warn('[region-dom-desync]', ...)` call and replaced the comment with a clear explanation that the sync is intentional. Kept the per-property comparison short-circuit (`if (el.style.left !== expectedLeft) el.style.left = expectedLeft`) so the DOM write only happens when needed. The state-overlap assertion from Phase 76 stays in place; only the DOM-desync warn is silenced. Considered a pre-render DOM sync alternative (write DOM before `setRegions`) but rejected as unnecessary refactoring — the microtask is well-understood, logically equivalent, and protects against any future code path that could create a desync.

* **[2026-05-19] Phase 78 — Revert Phase 77 `resetEl` left/width Clear (misdiagnosis):**
  Phase 77's microtask backstop reported `[region-dom-desync]` warnings on virtually every move/resize, with payloads consistently showing `domWidth: ''` (empty) and matching `expectedWidth`. Diagnosed Phase 77 Change 1 as the cause: clearing `el.style.width` to empty string in `resetEl` made the DOM empty BEFORE React's re-render, but React's reconciler only diffs **its own previous JSX memo** against the **new JSX value** — it has no awareness that the DOM was externally cleared. For a `move` (duration unchanged), prev memo == new value, so React skipped the DOM write and the width stayed `''`. The Phase 77 microtask then patched it and logged the warning. Reverted `resetEl` to only clear `transform`/`zIndex`/`filter`/`--track-color` — leaving drag-time `left`/`width` in the DOM. Now: for unchanged values the DOM is already correct (drag wrote the same value React last rendered); for changed values React's reconciler diffs prev memo vs new JSX, sees a difference, and writes correctly. Kept the Phase 77 microtask backstop active — it should now stay silent on healthy flows, and any future `[region-dom-desync]` log is a genuine repro of the original "stacked regions" symptom (which Phase 77 was speculatively chasing without a logged trace).

* **[2026-05-19] Phase 77 — DOM/State Sync After Cluster Commit ("zoom-to-update" bug):**
  User reported a purely visual stacking bug after resize-left cluster commit — state was correct (Phase 76 overlap assertion silent) but the DOM remained stale until zoom (any tiny zoom change) recomputed all `pixelsPerMeasure`-derived inline styles and forced a mass DOM update. Diagnosed as a React reconciler short-circuit on `style.left`/`style.width` for a region whose pre-drag rendered values and post-commit JSX values either matched memory or were missed by the diff. **Did NOT ship Gemini's plan as proposed** — Gemini blamed in-place mutation but the existing code (`applyDestructiveEdit`, `onUp` cluster build, `setRegions`) already creates fresh arrays/objects everywhere; immutability is already enforced. Instead applied a belt-and-suspenders DOM sync: (1) Extended `resetEl` in the window-drag effect to clear `el.style.left` and `el.style.width` to empty string after a drag commit (in addition to the existing `transform`/`zIndex`/`filter`/`--track-color` clears). Going from empty → numeric forces React to set the property on re-render. (2) Added a DOM/state-sync pass inside the existing post-commit `queueMicrotask` (alongside the overlap check from Phase 76). The pass walks `regionsRef.current`, finds each region's DOM node via `[data-region-id]`, compares `style.left`/`style.width` to `r.startMeasure * pixelsPerMeasure` / `r.durationMeasures * pixelsPerMeasure`, and if mismatched it both `console.warn`s `[region-dom-desync]` (so we still see if React missed an update) AND directly writes the correct values to the DOM. The captured `dragMode` is stamped into both telemetry payloads for diagnosis.

* **[2026-05-19] Phase 76 — Region Overlap Telemetry & Defensive Cull:**
  User reported occasional stacked/overlapping regions after multi-region resizes. Traced Gemini's exact verification scenario (3 flush regions, cluster resize-right) against the post-Phase-75 code and found the math produces a clean push-aside with no stacking. Gemini's Parts 1, 3, 4 are no-ops against the current code; Part 2 (cull of `durationMeasures <= 0`) cannot fire today because all paths produce `>= 1` due to integer measures and the `Math.max(1, ...)` resize clamps. **Did NOT ship Gemini's plan as-is — would have been speculative.** Instead, added two minimal changes in `onUp` after `setRegions`/`setNotes`: (1) **Defensive cull** of any region with `durationMeasures <= 0` plus any notes whose `regionId` no longer points to a surviving region — cheap insurance against future regressions. (2) **Dev-only overlap invariant**: a `queueMicrotask` that re-reads `regionsRef.current`, groups by track, sorts by `startMeasure`, and `console.warn`s any neighbor pair where `prev.startMeasure + prev.durationMeasures > cur.startMeasure`. When the user next reproduces the stacking bug, the warn payload will name the offending pair (IDs, startMeasure, durationMeasures, clipOffset, trackId, drag mode) so the real codepath can be targeted instead of guessed at.

* **[2026-05-19] Phase 75 — Negative clipOffset & Directional Cluster Sort:**
  Two follow-ups to Phase 74. (1) **Negative `clipOffset`:** Removed the bottle-origin lower bound from the resize-left clamp (anchor and companion) in `onMove`. The clamp `Math.max(0, initStart - initClipOffset)` was a `max` of two bounds — only the timeline-zero bound (`0`) still applies; the bottle-origin bound is dropped so `pendingClipOffset` is allowed to go negative when the user pulls the left handle past the bottle origin. Render math (`bottleOriginBeat = (r.startMeasure - r.clipOffset) * 4` plus the existing visibility filter) already handles negative values — no `RegionEditor.jsx` change needed. Result: the window can extend left into empty padding (notes stay locked to absolute grid; new empty measures appear before bottle beat 0). (2) **Directional cluster sort:** In `onUp`, swapped the static ascending `startMeasure` tie-breaker for a mode-dependent one — `resize-right` → descending, `resize-left` and `move` → ascending. The principle: the region whose moving edge intrudes into a neighbor must be processed LAST so the loop's last-wins semantics give it the territory. For resize-right with shared positive delta the LEFTMOST region's right edge intrudes rightward → it must be last → descending. Fixes the Phase 73 oversight where ascending sort silently undid the leftmost region's right-expansion in adjacent-cluster resize-right. Gemini's plan was structurally correct; nits caught and fixed: (a) Gemini said "remove the `Math.max(0, ...)` clamp" — actually only one of the two bounds in that `max` should go (the timeline-zero bound must stay), (b) Gemini's resize-left "ASCENDING" sort is the existing default — only `resize-right → descending` is a real change, (c) the external resolution loop (cluster vs background) does NOT need directional sort because cluster members are appended after the loop and order doesn't affect final results.

* **[2026-05-19] Phase 74 — Bottle/Window Non-Destructive Region Architecture:**
  Converted regions from destructive (shrink deletes notes) to a Window/Bottle model. Every region now carries a `clipOffset` (measures, default 0) and notes' `startBeat` is stored **bottle-local** (offset from the bottle origin = `region.startMeasure - region.clipOffset` measures). Resizing only crops/pans the visible window; notes are never destroyed.
    - **`applyDestructiveEdit` rewrite (`WorkstationShell.jsx`):** Cases B (right trim) and C (left trim) no longer delete notes — Case C advances `clipOffset` by `(newEnd - oldStart)` so the bottle origin stays put. Case D (middle split) deep-clones every note belonging to the original region into a new note ID linked to the right piece's new region ID, with the right piece's `clipOffset` advanced by `(newEnd - oldStart)`. Both halves independently hold the full original bottle and can be expanded outward to reveal hidden notes. Case A (total eclipse) still destroys the bottle.
    - **Resize-left math (`onMove`):** Pre-computes `minStart = max(0, initStart - initClipOffset)` so the window cannot expand left past either the timeline zero or the bottle origin. Tracks `pendingClipOffset = initClipOffset + (newStart - initStart)` so the bottle origin remains invariant during resize-left. Companions in cluster resize compute their own `initClipOffset`/`pendingClipOffset`.
    - **Move semantics (`onUp`):** The horizontal-move branch no longer shifts `note.startBeat` by `(pendingStart - initStart) * 4`. Bottle-local notes are invariant under window translation — only `trackId` updates on cross-track moves.
    - **`handleNoteAdd` (`WorkstationShell.jsx`):** New notes are now converted to bottle-local before storage: `bottleLocalStart = startBeat - (region.startMeasure - region.clipOffset) * 4`. The auto-create-region branch sets `clipOffset: 0` and stores `startBeat - measure*4`.
    - **`RegionEditor.jsx` rendering:** Each note's region is resolved by `regionId`. Notes outside the window `[clipOffset*4, (clipOffset+durationMeasures)*4)` return null. Visible notes render at `globalLeftPx = ((r.startMeasure - r.clipOffset) * 4 + n.startBeat) * ppb` — converting bottle-local back to global timeline pixels. Region highlights continue to render at `r.startMeasure * ppm`, so notes and highlights stay aligned.
    - **Copy/Paste:** Notes are no longer shifted by `(targetMeasure - leftmostMeasure) * 4` on paste — bottle-local positions are preserved verbatim; only IDs and trackId are remapped. The pasted region's new `startMeasure` handles the global shift implicitly.
    - **Region creation sites:** All four creation paths (`handleLaneClick`, lane `onDoubleClick`, `handleNoteAdd` auto-create, copy/paste — via `...r` spread) now emit `clipOffset: 0`.
    - **Critique of Gemini's plan:** Gemini's plan was conceptually sound but did not surface (a) the coordinate-space migration of `startBeat` from global to bottle-local, (b) the corresponding piano-roll render math change (timeline-region-local pixels would have offset notes from their region highlights), (c) the need to remove the move-time `startBeat` shift in `onUp`, or (d) the copy/paste `startBeat` shift no longer being valid. All four were caught and fixed in this implementation. Per-region-bottle (clone-on-split) was chosen over shared-`bottleId` linked clips for simplicity, per Dylan's explicit decision.

* **[2026-05-19] Phases 72–73 — Dynamic Grid Thinning & Adjacent Cluster Resize Fix:**

  * **Phase 72 — Grid Thinning at Macro Zoom (`computeGridBg`):** At extreme zoom-out `pixelsPerMeasure` could drop to 1–2px, causing all bar-line gradients to collapse into a solid gray block. Fix: added `labelStep = Math.max(1, Math.ceil(50 / ppm))` inside `computeGridBg` (matching the ruler's existing label-skip formula, no signature change). All three gradient intervals now use `effectivePpm = ppm * labelStep` as their base period. Beat and sub-beat layers are suppressed entirely when `labelStep > 1` — at macro zoom, those layers would be sub-pixel and add visual noise rather than information. Grid lines now perfectly align with visible ruler labels at every zoom level.

  * **Phase 73 — Adjacent Cluster Resize Undo Fix (`onUp` internal resolution):** When two flush regions on the same track were both selected and the user resize-dragged one into the other, the resize was silently undone. Root cause: the internal cluster resolution loop processes members in array order; the member processed LAST is always added to `resolvedCluster` intact (it is the "incoming"), while earlier members can be trimmed. Without sorting, the anchor (the region the user resized) could be processed first — trimmed back by the companion, completely negating the drag. Fix: `cluster.sort((a, b) => trackIndex diff → startMeasure diff)` in-place before the loop. Ascending `(trackIndex, startMeasure)` order means the rightmost member on each track is processed last, so it wins and correctly trims its left neighbors. In-place sort is safe because `cluster` is a fresh array built in `onUp`. Gemini's description ("ghost clones") was inaccurate — the real mechanism is pure processing-order semantics with no clone involved.

* **[2026-05-19] Phase 71 — Dynamic Minimum Zoom (No Void Past Project End):**
  The hardcoded minimum `zoomLevel` of `0.05` allowed zooming out past the point where the 200-measure project filled the viewport. Fix: inside both `onWheel` handlers (arrangement and piano roll), replaced `Math.max(0.05, ...)` with `Math.max(minZoom, ...)` where `minZoom = timelineRef.current.clientWidth / (PIXELS_PER_MEASURE * totalMeasuresRef.current)`. This derives the exact zoom floor at which the project width equals the viewport width — computed live inside the `setZoomLevel` callback using stable refs (no stale closure). Also added a `ResizeObserver` `useEffect` on `timelineRef` that re-clamps `zoomLevel` up to the new floor whenever the timeline container widens (browser window resize). Gemini's `window.innerWidth - leftColWidthRef.current` approach was rejected — `timelineRef.current.clientWidth` is the correct source as it accounts for scrollbar, borders, and flex layout without approximation.

* **[2026-05-19] Phase 70 — Cap Grid Width to Project Boundary:**
  When zoomed out, `timelineInner` (and the piano roll `grid` div in RegionEditor) had only `minWidth` set — preventing them from shrinking below the project width but not preventing them from expanding to fill the viewport. The `repeating-linear-gradient` filled the expanded div, drawing grid lines past measure 200. Fix: replaced `minWidth` with `width` on both divs so they stop exactly at `totalMeasures * pixelsPerMeasure`. Setting `width` inside an `overflow: auto` scroll container correctly allows scrolling when the viewport is narrower and stops content at the exact bound when wider. The parent `.timeline` div already has `background: var(--bg-base)` so the void area right of the project shows the clean base color. Same change applied to RegionEditor's grid div (`width: gridMinWidth`).

* **[2026-05-19] Phase 69 — Fix Label/Grid Misalignment (Root Cause of Phase 68 Regression):**
  Phase 68's `Math.round(ppm)` inside `computeGridBg` introduced a new bug: ruler labels are positioned at `i * pixelsPerMeasure` (the raw float, e.g. 37.5px) while the gradient repeated at `rPpm = Math.round(37.5) = 38px`. This compounds — at measure 10, the label is at 375px but the nearest grid line is at 380px (5px off), producing "numbers correct, grid strange." Fix: removed all rounding from `computeGridBg`. Used raw `ppm` for the measure-line gradient period and derived `ppb = ppm / 4` and `pp8 = ppm / 8` from it. CSS `repeating-linear-gradient` with a fractional period places lines at exact multiples (e.g. 0, 37.5, 75, 112.5, 150…) — perfectly matching ruler label positions with no accumulating drift. Sub-pixel antialiasing at fractional positions is imperceptible and far better than visible 5px misalignment. Gemini's "snap to multiple of 8" approach was rejected — it snaps only inside `computeGridBg` while labels still use the raw float, so the mismatch persists; also produces coarse zoom steps at low zoom levels.

* **[2026-05-19] Phase 68 — Fix Stale Hover Ref on Scroll & Sub-Pixel Grid Artifacts:**
  Two bugs fixed. (1) **Stale hover state:** Both `handleLaneClick` and the lane `onDoubleClick` were reading `hoverRef.current.measure` which is only updated by `onMouseMove`. After scrolling without moving the mouse, that ref is stale — the wrong measure is used and `isPositionOccupied` often aborts creation. Fix: replaced `hoverRef.current` reads with `Math.max(0, Math.floor((e.clientX - e.currentTarget.getBoundingClientRect().left) / pixelsPerMeasure))` computed live from the click event. Also removed the now-unnecessary `hoverRef.current.trackId !== trackId` guard in `handleLaneClick` and the `hTrackId === t.id` guard in `onDoubleClick`. Gemini's fix was correct in principle but suggested `e.nativeEvent.offsetX` which is relative to the event TARGET (unreliable when clicks bubble from child elements) — `getBoundingClientRect` approach is correct. (2) **Sub-pixel grid bars:** `computeGridBg` was passing fractional values (e.g. 37.5px) as gradient background-sizes. Browsers render fractional repeating-linear-gradient sizes inconsistently, producing thick/blurry lines. Fix: wrapped all three period values in `Math.round()`. Single change fixes both timeline and piano roll since `RegionEditor` imports the same function.

* **[2026-05-18] Phase 67 — Ghost/EditBtn Track Color Sync & Expand Zoom Limits:**
  Three UX polish items. (1) **Ghost color:** `.ghost` CSS was using `var(--accent-color)` — changed to `var(--track-color, var(--accent-color))`. No JSX change needed: ghost is a child of `.trackLane` which already has `--track-color` set as an inline style, so the variable cascades automatically. Gemini's suggestion to add an inline `style` to the ghost div was unnecessary. (2) **EditBtn color:** Same pattern — `.editBtn` border, text color, and hover background all changed from `var(--accent-color)` to `var(--track-color, var(--accent-color))`. Pure CSS, no JSX. (3) **Zoom limits:** Changed `Math.max(0.25, ...)` to `Math.max(0.05, ...)` in both `onWheel` handlers (timeline and piano roll). At `zoomLevel=0.05`, `pixelsPerMeasure=5px`, so 200 measures = 1000px — fits any screen. The existing ruler label thinning formula (`Math.max(1, Math.ceil(50 / pixelsPerMeasure))`) handles the extreme zoom correctly, yielding `labelStep=10` at min zoom.

* **[2026-05-18] Phase 66 — Sync Piano Roll Grid Width & Auto-Extend on Region Spawn:**
  Two bugs from Phase 65: (1) `RegionEditor.jsx` had its own `const MEASURES = 24` that was never updated — `gridMinWidth` was still capped at 24 measures regardless of `totalMeasures`. Fix: removed the constant, added `totalMeasures` to RegionEditor's props destructuring, changed `gridMinWidth = MEASURES * pixelsPerMeasure` → `gridMinWidth = totalMeasures * pixelsPerMeasure`, and passed `totalMeasures={totalMeasures}` from WorkstationShell's `<RegionEditor>` JSX. (2) Auto-extend (`setTotalMeasures`) was only wired to the drag `onUp` path. Added `if (measure + 1 > totalMeasures - 16) setTotalMeasures(prev => prev + 64)` to both `handleLaneClick` (single-click on empty track) and the lane `onDoubleClick` spawn block. Uses 16-measure threshold consistent with the drag handler.

* **[2026-05-18] Phase 65 — Extend Timeline to 200 Measures + Auto-Extend on Drag:**
  Replaced `const MEASURES = 24` with `const [totalMeasures, setTotalMeasures] = useState(200)`. Added `totalMeasuresRef = useRef(200)` and inline-synced it each render (same stale-closure pattern as `regionsRef`/`tracksRef`). `timelineInner` minWidth now uses `totalMeasures * pixelsPerMeasure`. Ruler label rendering uses an IIFE with label thinning: `labelStep = Math.max(1, Math.ceil(50 / pixelsPerMeasure))` — at low zoom levels only every Nth label renders, keeping ~50px minimum between visible labels (prevents overlap at 200 measures × small zoom). Auto-extend: in `onUp` inside `if (d.dragStarted)`, reads `totalMeasuresRef.current` (avoids stale closure vs `totalMeasures` state), and if `pendingStart + pendingDuration > totalMeasures - 16`, calls `setTotalMeasures(prev => prev + 64)`. CSS grid background is a `repeating-linear-gradient` and tiles infinitely — no change needed. Gemini's plan was structurally correct but missed the label thinning requirement.

* **[2026-05-18] Phase 64 — Fix Edit-Button Double-Click-Drag Spurious Region:**
  Gemini misdiagnosed this bug (claimed editBtn mousedown was bubbling to `startRegionDrag` — it was already stopped by `stopMouseDown`). The actual cause: editBtn's `stopMouseDown` prevents `dragRef.current` from ever being set, so the Phase 63 cooldown (`lastDragEndTimeRef`) is never stamped on editBtn drag-release. The browser still fires `dblclick` on the lane (common ancestor of editBtn + lane mouseup) after a drag. Fix: added `capturedRegionStartRef = useRef(null)` + `onMouseDownCapture` on the `.region` div (capture phase fires before child `stopPropagation` can suppress it). Restructured `onUp` signature to `(e)` and replaced `if (!d) return` early-exit with a proper `if (d) {...} else if (capturedRegionStartRef.current) {...}` branch. When no region drag was active but a region-subtree mousedown was captured and the cursor moved ≥4px, `lastDragEndTimeRef` is stamped — the existing 300ms guard in the lane's `onDoubleClick` then catches the stray dblclick. `capturedRegionStartRef` is always cleared at the bottom of `onUp`.

* **[2026-05-18] Phases 62–63 — Retain Selection on Drop & Fix Dbl-Click-Drag Race:**

  * **Phase 62 — Retain Selection on Drop:** Removed `setSelectedRegionId(null)` from inside the `if (d.dragStarted)` block in `onUp`. Previously that line cleared selection after every completed drag (Phase 57 / Phase 59 history). Removing it means the dragged region stays selected (`.regionSelected` → `brightness(0.8)`) after drop — gives continuous visual feedback about which region was last touched. Pure clicks continue to leave selection intact (Phase 59 rule unchanged). Empty-lane click still clears via `handleLaneClick`.

  * **Phase 63 — Fix Dbl-Click-Drag Race (Cooldown Guard):** Added `lastDragEndTimeRef = useRef(0)`. In `onUp`, when `d.dragStarted` is true, stamps `lastDragEndTimeRef.current = Date.now()` after removing `is-dragging`. In the lane's `onDoubleClick`, added `if (Date.now() - lastDragEndTimeRef.current < 300) return` as the first check (before the region guard and before `setRegions` / `setEditingTrackId`). Root cause: the browser fires `dblclick` on the deepest common ancestor of both clicks; when the second mouseup lands at a different position than the first (drag case), the common ancestor is the lane, not the region — bypassing the existing `e.target.closest('.region')` guard. The timestamp cooldown catches this because `dblclick` fires ~0ms after `onUp`, so a 300ms window reliably blocks the stray event while remaining imperceptible to deliberate double-click-then-create workflows.

* **[2026-05-18] Phases 58–61 — Workstation Drag, Selection & Region Deletion:**

  * **Phase 58 — Fix Double-click Region Spawn Bug:** Diagnosed root cause: `startRegionDrag` was calling `document.body.classList.add('is-dragging')` on every mousedown, immediately applying `pointer-events: none` to all `.region` elements via CSS. On a double-click, this created a race where the browser could resolve the `dblclick` target against the lane instead of the region, bypassing the region's `stopPropagation` and triggering the lane's `onDoubleClick` which created a spurious region. **Fix 1 (root cause):** deferred `is-dragging` and cursor change from `startRegionDrag` to the `onMove` handler — only applied after the 2D drag threshold is crossed (`dragStarted: false` field on `dragRef`). **Fix 2 (defensive):** added `if (e.target.closest(`.${styles.region}`)) return` guard to the lane's `onDoubleClick` so even if a dblclick escapes a region, no spurious creation occurs. Gemini's proposed fix (adding `stopPropagation` to region onClick/onDoubleClick) was rejected — those were already present.

  * **Phase 59 — Region Deletion via Delete/Backspace:** Added keyboard-driven region deletion. **Problem identified:** `setSelectedRegionId(null)` was called unconditionally in `onUp`, so selection was always cleared on mouseup — a keyboard handler watching React state would never have a target. **Fix:** moved `setSelectedRegionId(null)` inside the `if (d.dragStarted)` block so completed drags clear selection as before, but pure clicks leave the selection intact. Added `selectedRegionIdRef = useRef(null)` and inline-synced it each render (same pattern as `regionsRef`/`tracksRef`). New `useEffect` with `[]` dep listens for Delete/Backspace, reads `selectedRegionIdRef.current`, filters both `regions` and `notes` (by `regionId` foreign key) in two pass operations, then clears selection. Input-focus-guarded. Kept separate from spacebar `useEffect` since that one re-registers on `handlePlayPause` changes. Gemini's plan was rejected for listing `selectedRegionId` as a dep (would re-register on every click) and `setRegions`/`setNotes` as deps (they're stable setState refs).

  * **Phase 60 — Fix Vertical Drag Threshold (2D Distance):** The `dragStarted` threshold check only measured X-axis displacement (`Math.abs(e.clientX - d.startX) < 4`). Dragging straight up or down to a different track barely moved X, so the threshold was never crossed and the drag never initiated — region stayed frozen during vertical-only movement. Fix: added `startY: e.clientY` to `dragRef.current` in `startRegionDrag`, changed threshold to `Math.hypot(e.clientX - d.startX, e.clientY - d.startY) < 4` (2D Euclidean). Gemini's `Math.hypot` fix was correct but critically omitted `startY` from `dragRef` — the expression would have evaluated to `NaN < 4 = false`, firing drag immediately on any mousedown.

  * **Phase 61 — 3-Tier Region Highlighting:** Three distinct visual states driven by CSS and minimal inline style. **Normal:** base pastel color. **Selected** (click, no drag): `filter: brightness(0.8)` via `.regionSelected` (changed from prior 0.7). **Active/Dragging** (mousedown held or in-flight drag): `filter: brightness(0.6)`. Implementation: merged `filter: brightness(0.6)` into the existing `.region:active` rule (which previously only set `cursor: grabbing`) — no new selector, no `!important` since `.region:active` specificity `0-2-0` already beats `.regionSelected` `0-1-0`. Inline `d.el.style.filter = 'brightness(0.6)'` applied at drag threshold crossing to persist the dark state after browser drops `:active` mid-drag; cleared in `onUp` alongside other inline style resets.

* **[2026-05-16] Phases 51–57 — Workstation UX & Bug Fixes:**

  * **Phase 51 — RegionEditor Tab Layout Refinement:** Renamed "NOTES" tab label to "PIANO ROLL" (state value `'notes'` unchanged). Fixed tab rendering: inspector + colResizer always visible on all tabs. `.pianoRoll` scroll container always mounted (preserving `scrollTop` natively). Piano keys always rendered inside it. Only the note grid vs. placeholder is conditionally swapped via ternary inside `.pianoRoll` — `.placeholder { min-height: 0 }` prevents the 2376px grid height from inflating the container. Edit button in top-right of inspector now always accessible regardless of active tab.

  * **Phase 52 — Bottom Transport Bar:** Moved play/pause, stop, record buttons from the top bar's `.transportCenter` to a new 48px `.bottomTransport` bar at the very bottom of `.shell` (last flex child). Top bar now contains only navigation (theme toggle, home) and meta (BPM, TIME). `.bottomTransport` uses correct CSS vars `--bg-panel` + `--border-mid`; reuses existing `.transportBtn` / `.transportBtnActive` classes. `.transportCenter` CSS class removed (unused).

  * **Phase 53 — Drag Interference Fix (hover suppression):** `document.body.classList.add('is-dragging')` added on both `startRegionDrag` and ruler scrub `mousedown`; removed in their respective `onUp`/cleanup handlers. CSS `:global(body.is-dragging) .region { pointer-events: none }` suppresses region `:hover` states (including the edit button) for the duration of any drag. `handleLaneMouseMove` guards against `dragRef.current` in addition to `isDraggingRef` — stops ghost from animating during cross-track region drags.

  * **Phase 54 — Live Region Color During Cross-Track Drag:** Inside the `onMove` handler, when `noOverlap` passes for a `move` drag, `d.el.style.setProperty('--track-color', color)` is called only when `candTrackId !== prevTrackId` (one call per track transition, not every frame). Uses `tracksRef.current.find(...)` — never stale `tracks` state. `d.el.style.removeProperty('--track-color')` in `onUp` lets React's post-drop re-render own the final color.

  * **Phase 55 — Fix Note Shifting on Left-Edge Resize:** `onUp` gated `setNotes` update behind `d.mode === 'move'` — resize-left no longer shifts notes left when the region boundary extends. (resize-right was already safe: `pendingStart` unchanged → `beatDelta === 0`.)

  * **Phase 56 — Selected Region Darker Tint:** Added `selectedRegionId` state. `startRegionDrag` sets it (covers move + both resize handles). `handleLaneClick` clears it (regions stopPropagation clicks so this only fires on empty space). Conditional `.regionSelected` class applies `filter: brightness(0.7)` — works with the dynamic `--track-color` pastel system without hex math.

  * **Phase 57 — Deselect Region on Drag End:** `onUp` unconditionally calls `setSelectedRegionId(null)` alongside `dragRef.current = null` — region deselects after any completed drag (move, resize-left, resize-right, same-track, cross-track).

* **[2026-05-16] Phase 50 — RegionEditor Navigation Tabs:** Added a three-tab strip to the previously-empty `editorBar` (48px separator above the piano roll body): **notes**, **instrument**, **effects**. Local `activeTab` state in `RegionEditor.jsx` — no prop threading needed. Tab buttons rendered via a `.map` over a string array; active tab gets a `--track-color`-colored `border-bottom` underline. Key architectural decision: the piano roll scroll container is **never unmounted** on tab switch — `display: none` (`.hidden` CSS class) hides it instead, so `scrollTop` is preserved natively. The inspector and column resizer remain always visible (they are the persistent left sidebar); only the piano roll's `.pianoRoll` div is hidden when off the notes tab. Instrument and Effects panels render placeholder text in the piano roll's flex slot (`flex: 1`). Tabs are centered in the bar via `justify-content: center` on `.editorBar`. The C4-default scroll `useEffect` fires only on `track?.id` changes — tab switches never re-trigger it.

* **[2026-05-16] Phase 49 — Region Container Logic (notes ↔ regions linked via `regionId`):** Notes now carry a `regionId` foreign key: `{ id, trackId, note, startBeat, durationBeats, regionId }`. `handleNoteAdd` in `WorkstationShell` reads `regionsRef.current` (inline-synced ref, no stale closure) to find an existing region covering `startBeat` via `startBeat >= r.startMeasure * 4 && startBeat < (r.startMeasure + r.durationMeasures) * 4`. If found, the note joins it; if not, a new 1-measure region is auto-created at `measure = Math.floor(startBeat / 4)`. The drag `onUp` handler extends the commit to also `setNotes` when `beatDelta !== 0 || trackChanged`: `n.startBeat += beatDelta` (`measureDelta * 4`, not `measureDelta` — Gemini's bug) and `n.trackId = pendingTrackId` for cross-track drags (preventing notes from becoming invisible in the new track's editor). Both `initStart` and original `trackId` were already stored in `dragRef` — no new drag state needed. No changes to `RegionEditor.jsx` or CSS.

* **[2026-05-16] Phase 48 — Per-Track Color Differentiation:** Each track now receives a distinct color from a 7-color mid-saturation DAW-style palette (`TRACK_COLORS` module-level constant in `WorkstationShell.jsx`). Color assigned in `handleAddTrack` via `TRACK_COLORS[(n - 1) % TRACK_COLORS.length]` using the stable `nextIdRef` counter — no stale closure risk. `color` field added to track object (`{ id, name, instrument, color, isMuted, isSolo }`). `--track-color` CSS variable injected via inline `style` on both `trackRow` (left header column) and `trackLane` (right timeline column) — both are required because CSS variables cascade downward and the two divs are in separate DOM subtrees. Color dot: `<span className={styles.trackColorDot} style={{ background: t.color }} />` inside a new `.trackNameRow` flex wrapper preceding the track name. `.trackName`, `.trackRowActive` background/box-shadow, and `.region` background/border all updated to `var(--track-color, var(--accent-color))`. `RegionEditor` root div injects `style={{ '--track-color': track?.color }}`; `.noteBlock` and `.regionHighlight` in `RegionEditor.module.css` updated to `var(--track-color, var(--accent-color))`. Palette: `#5DCAA5` teal, `#5A9FD4` blue, `#D4845A` coral, `#A57BD4` purple, `#D4C45A` amber, `#D45A7B` rose, `#7BD45A` lime — all chosen for visibility on `#0e0e10` dark background. Colors are hardcoded hex (not CSS vars) so they remain consistent in both light and dark themes.

* **[2026-05-16] Phase 47 — Piano Roll Default Scroll + Persistence:** Piano roll now defaults to showing C4 at the bottom of the viewport on first open (index 59 in the 132-key `KEYS` array; `scrollTop = 59 * 18 - clientHeight + 18`). Scroll position persists across track switches and editor close/reopen via `lastPianoScrollTopRef = useRef(null)` in `WorkstationShell`. `handlePianoRollScroll` saves `e.target.scrollTop` to the ref on every scroll event. `lastPianoScrollTopRef` passed as `scrollMemoryRef` prop to `RegionEditor`. The C4 `useEffect` (dep: `track?.id`) checks `scrollMemoryRef.current !== null` first — restores saved position if present, else calculates C4. `null` is the sentinel for "no user scroll yet." Page refresh resets the ref to null, restoring the C4 default. `0` is a valid scrollTop (fully scrolled up) and `0 !== null` correctly triggers restoration.

* **[2026-05-16] Phase 46 — Interactive BPM Control + Playhead Anchoring:** `BPM` module-level constant replaced with `bpm` state (`useState(120)`) + `editingBpm` / `tempBpm` edit states in `WorkstationShell`. `pxPerSec = PIXELS_PER_BEAT * (bpm / 60) * zoomLevel` now derives from state, so all downstream consumers (`updatePlayhead`, `seekToClientX`, `handlePlayPause`) automatically pick up BPM changes via React's dependency chain. Click-to-edit BPM: the transport bar `metaValue` span becomes an `<input type="number">` on click with `autoFocus`; Enter/blur commits, Escape reverts. `handleBpmCommit` validates + clamps (20–300). **Playhead anchoring:** before applying a new BPM, `Transport.seconds` is scaled by `bpm / n` to preserve the current musical (bar:beat) position — prevents playhead from jumping on tempo change. Uses `Tone.Transport.bpm.value = n` (instant assignment, not `rampTo`) because this is a discrete user action and `rampTo` would create ordering drift with the simultaneous `seconds` adjustment. `.bpmInput` CSS matches `.metaValue` exactly with spinners hidden.

* **[2026-05-16] Phase 45 — Piano Roll Note Placement Scroll Offset Bug:** Fixed `handleGridClick` in `RegionEditor.jsx` — the Y coordinate calculation was `(e.clientY - scrollRect.top)` which ignored vertical scroll position, placing notes at the wrong pitch when the user had scrolled down. Added `+ scroll.scrollTop` to match the existing X formula which already correctly adds `+ scroll.scrollLeft`. One-token fix on line 128.

* **[2026-05-15] Phase 44 — Cross-Track Region Dragging + Multi-Region Workflow:** Extended `WorkstationShell.jsx` with a complete multi-region, cross-track editing workflow. **Multi-region support:** flat `regions` state array (one `setRegions` on commit, never nested in tracks); `nextRegionIdRef` provides stable IDs. **Ghost + collision:** per-lane ghost div always in DOM, opacity controlled imperatively; hidden on populated tracks and on occupied measures via `isPositionOccupied`. **Hybrid click/dblclick creation:** empty track = single-click creates region; populated track = double-click creates at hovered measure only if unoccupied; both paths guarded by `hoverRef` and `isDraggingRef`. **AABB physical-wall drag collision:** `noOverlap(candTrackId, candStart, candDur)` tests the candidate track — `move` passes the Y-detected `candTrackId`, resize passes the original `d.trackId`. Physical-wall behavior: `newStart`/`newDuration` default to `d.pendingStart`/`d.pendingDuration` (last valid), so the region stops at the boundary rather than snapping back on release. **Cross-track dragging:** Y detection uses scroll-corrected formula `e.clientY - rect.top + timelineRef.scrollTop - RULER_HEIGHT`, floored by `TRACK_H = 72` — accounts for ruler height and scrolled position. `tracksRef` (inline-synced, same pattern as `regionsRef`) prevents stale-closure reads of `tracks` state inside the `useEffect` drag handler. Visual: `translateY((pendingTrackIndex - origTrackIndex) * TRACK_H)` + `zIndex: 10` during drag; both cleared on `mouseup` before committing `trackId: d.pendingTrackId` to React state. Added `TRACK_H = 72` and `RULER_HEIGHT = 24` constants. **Region highlights in piano roll:** `RegionEditor` receives `regions` prop (filtered to editing track); renders `.regionHighlight` divs (`opacity: 0.08` accent overlay, `pointer-events: none`, `z-index: 1`) marking clip boundaries behind MIDI notes. Piano roll key range expanded from C1–C6 to C-2–C8 (132 keys) for full MIDI coverage.

* **[2026-05-15] Phase 43 — Piano Roll Editor Shell:** Built `src/components/Workstation/RegionEditor/` — bottom-docked panel rendered when `editingRegion` is set. Inspector column (260px) shows region id, track name + instrument, and a theme-aware `<select>` for instrument switching (visual only). Piano roll uses programmatically-generated C1–C6 (72 chromatic keys at 18px each) with white/black row backgrounds via `--piano-key-white` / `--piano-key-black` vars — these are directionally correct across themes (the previous `--bg-panel` / `--bg-base` mapping inverted in light mode where `--bg-panel` is actually darker than `--bg-base`). Grid uses six layered `repeating-linear-gradient`s on `.grid`: bar lines (200px), sub-beat lines (50px), octave lines (216px), semitone rows (18px), accidental-row shading, natural-row shading. Two new theme vars `--pr-shade-natural` / `--pr-shade-accidental` invert which rows get shaded per theme — dark mode brightens naturals (`rgba(255,255,255,0.04)`), light mode darkens accidentals (`rgba(0,0,0,0.06)`); the other shade is `transparent` in each theme so they never conflict. Grid alignment fix: explicit `minHeight: KEYS.length * KEY_H` (= 1296px) on both keys column and grid div — `align-items: stretch` inside an `overflow: auto` flex container is bounded by visible cross-size, not the line's natural maximum, so flex stretch alone left the grid blank below the viewport on scroll.

* **[2026-05-15] Phase 42 — Region Lifecycle (Ghost, Drag, Resize, Edit):** Implemented full region editing workflow in `WorkstationShell.jsx`. **Ghost region:** ref-based per-lane preview that snaps to measure boundaries on `mousemove` via direct DOM mutation (zero React re-renders during hover). Only renders on lanes with no existing regions (`laneIsEmpty(trackId)`); suppressed during ruler scrub via `isDraggingRef`. **Single-click commit:** lane click reads `hoverRef` and pushes a new region; lane `mousedown` / `click` stop propagation so the timeline seek handler doesn't fire on lane interactions (cleaner separation: ruler = transport, lanes = content). **Move + resize:** `dragRef` holds `{ regionId, mode, startX, initStart, initDuration, el, pendingStart, pendingDuration }`. Body mousedown = `mode='move'`; 6px left/right edge handles = `resize-left` / `resize-right`. Window-level `mousemove` writes directly to `region.style.left/width` (zero re-renders during drag); `mouseup` commits exactly one `setRegions` call. Clamping: `move` floors start at 0, `resize-right` floors duration at 1, `resize-left` clamps so left edge stops at 0 and at right-edge − 1. Z-index: edit button (4) > resize handles (3) > region (1) — edit stays clickable above the right handle. **Hover edit + editor panel:** pure CSS `:hover` reveals an `[ edit ]` button on each region; click sets `editingRegion`, opening `<RegionEditor>` in a resizable bottom-docked panel. Divider drag uses the same ref-based pattern with `editorWrapRef.style.height` writes during drag and one state commit on release; clamped to `[150, vh - 200]`. **Spacebar transport** (input-focus-guarded, `e.preventDefault()` stops page scroll), `MM:SS:CC` time display updated via `timeRef.current.textContent` in the existing rAF loop.

* **[2026-05-14] Phase 41 — Workstation Visual Shell (Transport + Tracks + Timeline):** Built the Workstation DAW shell behind a `[ continue → ]` gate on the under-construction warning page. `WorkstationShell.jsx` lays out a 56px transport bar (theme toggle, home button, play/stop/record cluster, BPM + MM:SS:CC time display), a 220px track-header column aligned to the timeline ruler via a 24px `.tracksHeader` spacer, and a horizontally-scrollable timeline grid (2400px = 24 measures × 100px) with a sticky `position: sticky; top: 0` ruler. Tracks are React state (`useState([])`) with monotonic `nextIdRef` for stable names across hypothetical delete/add cycles. Track headers show name + instrument (theme-aware `<button>` selector placeholder) + per-track M/S toggles + fake volume slider; matching 72px lanes in the timeline grid stay vertically aligned with headers. Playhead is a 1px mint line driven by a single rAF loop reading `Tone.Transport.seconds`; `handlePlayPause` toggles based on `Tone.Transport.state` (start ↔ pause keeps position), `handleStop` resets to zero. `PIXELS_PER_BEAT * (BPM/60) = 50 px/sec` at 120 BPM keeps the playhead crossing one CSS bar line every 2 seconds regardless of BPM (future-proof for BPM control). Mousedown on `.timeline` seeks via `seekToClientX`; mousedown originating in `rulerRef` ALSO begins a drag-scrub (window-level mousemove). Empty state renders a `no tracks / add a track to begin` block with a primary `[ + add track ]` CTA; populated state shows tracks plus a dashed-border ghost `[ + add track ]` footer button. **Known limitation flagged:** `Tone.Transport` is a global singleton shared with `useAudioEngine` — workstation and VoxTool both call `.start()` / `.stop()` independently; a coordination layer (`useTransport` hook) is the future fix when global timeline math comes online.

* **[2026-05-12] Phase 40 — VoxDAW Homepage + Routing + Theme Persistence + Typography Pass:** Replaced single-page App with a state-routed shell. `src/Root.js` owns `page` state (`'home' | 'voxtool' | 'workstation'`) and `isDarkMode` state, applies `data-theme="light"` on a top-level wrapper, and threads `onNavigateHome` / `isDarkMode` / `onThemeToggle` to each page. `HomePage` (`src/components/HomePage/`) is a dark-premium landing page with VoxDAW wordmark, alpha disclaimer (folded in from the removed WelcomeModal), and two CTAs routing to VoxTool or Workstation. `Workstation` warning page guards entry to the WIP shell with a continue button. **No new dependencies** — Gemini's `react-router-dom` suggestion rejected as overkill for a 2-page app and risky (would unmount/remount all 7 VoxTool hooks on navigation, re-prompting camera permission, re-downloading the MediaPipe model). **Theme persistence** survives navigation since state lives at Root. **Typography pass:** surgical font-size bumps (~40 declarations across 8 module CSS files) — section titles 7→9px, labels 9→11px, engage button 10→12px, etc. Gemini's "convert rigid heights to min-heights" advice rejected as dangerous to the existing DAW aesthetic. **Light-mode contrast pass:** replaced hardcoded `#5DCAA5` / `rgba(93,202,165,...)` / `rgba(255,255,255,...)` colors throughout Workstation, HomePage, and overlay terminals with theme-aware CSS variables. Root cause of multiple early "inverted" appearance bugs: light-mode `--bg-panel` is darker than `--bg-base` (opposite of dark mode), so any code that assumed "panel = always lighter" flipped contrast direction across themes. **Global text-selection disable:** added `user-select: none` to `*, *::before, *::after` in `index.css` with `auto` exception for `input, textarea, select`. **WelcomeModal removed** — homepage now serves as the single entry gate.

* **[2026-05-09] Phase 39 — Hard-Tune Autotune Engine:** Added a real-time pitch correction (T-Pain hard-tune) module. `useAutotune.js` hook uses `Tone.UserMedia` for mic input, a native `AnalyserNode` as a dead-end tap on the raw signal (before pitch shifting), `Tone.PitchShift` (windowSize: 0.04) for the shift, and `pitchfinder.YIN` (2048-sample buffer, 44.1 kHz calibrated) for pitch detection. Detection runs in a rAF loop gated to ~30 fps (every 2nd frame) to cap YIN's O(n²) CPU load. Snap-to-scale uses existing `snapToNearest(hz, Float32Array)` from `dsp.js` with `SCALES` from `scales.js` — no MIDI intermediate. Semitone shift computed as `12 * Math.log2(targetHz / detectedHz)` and assigned directly to `pitchShift.pitch` (no rampTo = hard snap). `AutotuneTerminal` component: draggable overlay at `bottom: 12px; left: 320px` (right of VocoderTerminal), shows in/out note readouts updated imperatively by the rAF loop (zero React state), plus single wet/dry DAW fader. `[ autotune ]` toggle added to Controls > Output. Scale changes in Controls now also call `setAutotuneScale` via a `handleScaleChange` wrapper in `App.js`. Gemini's incorrect analyser placement (after PitchShift) and unnecessary MIDI-note-number conversion were rejected in favor of the above approach.

* **[2026-05-08] Phase 38 — Fix Fluid Arp Pause (Synchronize rAF + Scheduler):** The previous fix (Phase 37 — 5% threshold) eliminated the stutter but introduced a pause: any call to `pat.interval = x` from the rAF loop triggers `_rescheduleEvents()` which resets Tone.js's countdown timer. While the hand tilts continuously, the countdown kept resetting before completing, so no note ever fired. Fix: added `fluidUpdateReadyRef = useRef(false)`. The Pattern callback sets it `true` after firing a note in fluid mode. The rAF loop only updates `pat.interval` when the flag is `true`, then immediately resets it to `false`. This guarantees exactly one `_rescheduleEvents()` call per note cycle — after the note fires and before Tone.js queues the next one — so the countdown is never interrupted mid-flight. `smoothedFluidIntervalRef` continues to lerp toward the tilt target every frame; the flag just gates when that value is pushed into the scheduler. Flag reset added to `stopAudio`.

* **[2026-05-08] Phase 37 — Fix Fluid Arp Rate Stutter:** The `[ speed snap ]` off (fluid) mode was stuttering because `arpPattern.interval = fluidTime` was assigned from inside the `Tone.Pattern` callback itself. Tone.js calls `_rescheduleEvents()` when `interval` changes — doing this from within the scheduler tick corrupts the lookahead queue (double-fires, missed beats). Fix: removed the self-assignment from the Pattern callback entirely; moved `pat.interval = smoothed` to the rAF loop (safe context, outside Tone.js's tick). Added a 5% relative threshold (`Math.abs(smoothed - prevNum) / prevNum > 0.05`) so the scheduler is only updated 10–20 times/second during a sweep instead of 60fps. `appliedArpRateRef` is repurposed to hold the last-applied float in fluid mode (was hardcoded `null`); the snap-mode delta check (`rate !== appliedArpRateRef.current`) still works correctly since string/number comparison is always `true` on mode transition. Zero new refs or nodes needed. Gemini's `Tone.Clock` suggestion was rejected as unnecessary complexity.

* **[2026-05-08] Phase 36 — ArpTerminal: Reverb Fader:** Added a `rvrb` wet/dry fader (0–100%) to the arp signal chain and ArpTerminal UI. Used `Tone.Freeverb` (synchronous init, no `await .generate()`) inserted between `arpDelayRef` and the bypass/fx split (`arpBypassGainRef`/`arpFxGainRef`). New chain: `arpVoice → arpVol → arpBaseVol → arpDelay → arpReverb → [arpBypassGain | arpFxGain]`. `arpReverbRef` and `arpReverbWetRef` (persists wet value across engage/disengage) added alongside other arp refs. `setArpReverb(value)` callback writes `arpReverbWetRef` and calls `wet.rampTo(value, 0.1)`. Node disposed and nulled in the `stopAudio` timer block alongside other arp nodes. Gemini's insertion point (before `arpVolRef`) was rejected — reverb belongs downstream of delay, not upstream of the gesture-controlled volume. Terminal widened 228px → 268px for 5-fader layout. Props threaded `useAudioEngine → App.js → Viewport → ArpTerminal` with no mirrored React state.

* **[2026-05-08] Phase 35 — New Arp Pattern Modes in Gesture Routing:** Added 5 new first-class arp modes (`simpleUp`, `simpleDown`, `complexUp`, `complexDown`, `complexDownUp`) covering previously missing Tone.Pattern traversal types (`'up'`, `'down'`, `'downUp'`). Each mode is a full entry in `ARP_PATTERN_TYPE`, `ARP_MODE_LABEL`, `ARP_DEST_TO_MODE`, and `buildArpNotes` (simple/complex variants collapse into shared `switch` fall-through cases). Corresponding dest IDs added to `TRIGGER_DESTINATIONS` under a new `'arp_pattern'` group in `gestureMappings.js`; `TRIGGER_GROUP_LABELS` gains `arp_pattern: 'Arp Pattern'`. They appear as a new optgroup in the GestureSettings gates dropdown automatically. `DEFAULT_TRIGGER_MAPPINGS` untouched — defaults unchanged. Zero changes to Phase 3 Execute logic; existing delta-check handles new modes automatically. Gemini's pattern-override approach (`pat.pattern = 'down'` outside the mode system) was rejected: it fights the delta-check which overwrites `pat.pattern` every time mode/root/third changes.

* **[2026-05-08] Phase 34 — ArpTerminal: Base Volume Fader:** Added a static `vol` fader (−36 to +6 dB) to ArpTerminal for balancing arp level against the chord engine. Critical fix over Gemini's plan: Gemini wrote to `arpVolRef` which is owned by the gesture routing loop (`case 'arp_volume'` in `updateParams`), causing the slider to be overwritten ~60×/sec. Correct approach: inserted a new `arpBaseVolRef` (`Tone.Volume(0)`) into the signal chain between `arpVolRef` (gesture) and `arpDelayRef` — each node has exactly one writer. `setArpBaseVolume(db)` callback calls `arpBaseVolRef.current.volume.rampTo(db, 0.1)`. Terminal widened 190px → 228px for 4-fader layout. Props threaded `App.js → Viewport → ArpTerminal` with no mirrored React state (audio engine is source of truth).

* **[2026-05-08] Phase 33 — ArpTerminal: Dedicated Instrument + Decay Fader:** Added two new controls to the ArpTerminal popup. New `makeArpVoice(name)` module-level factory in `useAudioEngine.js` produces a fresh monophonic Tone.js synth per instrument type: `analog` (`Tone.Synth` triangle), `fm` (`Tone.FMSynth`), `am` (`Tone.AMSynth`), `pluck` (`Tone.PluckSynth` Karplus-Strong). `applyArpDecay(voice, value)` helper maps the decay slider (0.1–2.0s) to `envelope.decay` + proportional `release` for all types, or to `resonance` (0.05–0.98) for `PluckSynth`. `startAudio` now calls `makeArpVoice(arpInstrumentNameRef.current)` + `applyArpDecay` instead of a hard-coded `Tone.Synth` — persists selection across engage/disengage cycles. New `setArpInstrument(name)` callback hot-swaps the running voice by creating the new synth, connecting it to `arpVolRef`, then disconnecting and disposing the old one — no audio gap. New `setArpDecay(value)` callback writes to `arpDecayRef` and calls `applyArpDecay` imperatively. Both follow the existing `useCallback`-over-refs pattern (no `useEffect`, no re-renders). `ArpTerminal.jsx` gains an `<select>` instrument dropdown above the fader row plus a third `dcay` vertical fader. Terminal widened 160px → 190px. Props threaded: `App.js → Viewport → ArpTerminal` (`arpInstrument`, `onArpInstrumentChange`, `onArpDecayChange`). Chord engine completely unaffected.

* **[2026-05-08] Phase 32 — Always-On Welcome Modal:** Created `src/components/WelcomeModal/` (`WelcomeModal.jsx` + `.module.css`). `position: fixed; inset: 0; z-index: 300` overlay blocks all DAW interaction on every page load. Controlled by `showWelcome` state (`useState(true)`) in `App.js` — no localStorage, so it resets on every reload. No backdrop-click dismissal; user must explicitly click `[ ENTER STUDIO ]`. Content: Alpha disclaimer + privacy note (all video processing local, nothing sent to any server). Styled to match `MidiModal` pattern exactly — `var(--bg-panel)` box, `var(--overlay-bg)` backdrop, `var(--accent-color)` borders and CTA, `var(--text-primary)` body text. Privacy block uses a mint left-border accent block. Fully theme-aware (light/dark).

* **[2026-05-06] Phase 31 — Light/Dark Mode Toggle + Collapse Tab Fix:** Added a full CSS custom property theming system supporting dark (default) and light modes with zero re-renders — `data-theme="light"` attribute set on `.app` div drives all overrides via `[data-theme="light"]` block in `index.css`. Defined 17 theme variables in `:root` (`--bg-base/panel/elevated/surface/frosted`, `--border-subtle/faint/mid/strong`, `--text-primary/muted/dimmer/dimmest`, `--overlay-bg`, `--input-bg`, `--row-bg/border`) plus 5 accent/legibility variables (`--accent-color`, `--form-text`, `--form-text-mid`, `--form-text-dim`, `--warning-color`) and 5 viewport/piano-roll variables (`--bg-camera`, `--piano-key-white/white-bdr/black/black-bdr`). Light mode palette: warm paper (`#f0ede8` base), `#1a6b55` dark teal accent (5:1 contrast ratio on beige), charcoal text. Dark mode values identical to prior hardcoded colors — zero dark mode regressions. All 8 chrome UI CSS modules updated to use vars (`Controls`, `TelemetryHUD`, `GestureSettings`, `MidiModal`). Overlay panels (ArpTerminal, VocoderTerminal, PianoRoll, LoopProgress) stay dark regardless of theme for camera legibility — except `PianoRoll` keys which adapt (`#f5f2ed` white keys / `#52525c` black keys in light mode). `Viewport` gains `background: var(--bg-camera)` (`#dedad3` warm gray in light) and `.label` switches to `var(--form-text-mid)`. `◑`/`○` toggle button added to Controls header flex row; `isDarkMode` state in `App.js` wired through `onThemeToggle` prop. **Collapse tab fix:** The `leftToggleBtn` (absolute at `top: 16px; left: 16px; z-index: 50`) was positioned directly on top of the Controls header `themeBtn` (same screen coordinates), burying it. Replaced with the pre-designed `.collapseTab` CSS class (`position: absolute; left: 100%`) that protrudes from the sidebar's right edge — follows the sidebar's `translateX` transform so it remains accessible at the left viewport edge when the sidebar is collapsed. `leftToggleBtn` fully removed from `App.js` and `App.css`. `onCollapseToggle` prop added to Controls.

* **[2026-05-06] Phase 30 — 6-Voice Polyphony + Complex Chord Library:** Expanded chord voices from 4 to 6 (`analogVoicesRef` and `stringsVoicesRef` each grow to 6 entries). `INIT_FREQS` expanded to 6 entries; re-engage path fixed (`INIT_FREQS[i]` instead of `INIT_FREQS[i % 4]`). `resolveChord(dest)` rewritten from `{ voice13, voice7, thirdST, seventhST }` boolean gates to `{ intervals: number[], thirdST }` — intervals are semitone offsets from root (can be negative for sub-octave). Phase 3 Execute replaced with a `for (let i = 0; i < 6; i++)` loop: voices up to `intervals.length` get `rampTo(freq)` and `rampTo(VOICE_DB)`; remaining voices mute to `VOICE_MUTE`. `setInstrument` generalized to 6-voice loop using `const semitones = [0, ST_MAJ3, ST_P5, ST_MIN7, 12, 12+ST_MAJ3]`. New constants: `ST_P4 = 5` (perfect 4th), `ST_DIM5 = 6` (diminished 5th). `CHORD_DEST_LABELS` expanded from 6 to 18 entries (kept `chord_root_7` and `chord_root_maj7`; added sus4, dim, maj/min +oct, maj/min sub-bass, root −2 oct, poly maj/min, maj9/min9). `gestureMappings.js` gains a `complex_chord` optgroup with 11 chord IDs (all 9 original + chord_maj_9 + chord_min_9) and `chord_root_maj7` in the `chord` group. `GestureSettings` gates tab shows the new Complex Chord optgroup between Chord and Arp Mode. Added `right_ring_pinky` and `left_ring_pinky` combo sources (group: `'right'`/`'left'`) — both fully enumerated in strict 8-state combo detection. `DEFAULT_TRIGGER_MAPPINGS` expanded to 17 rows: `right_ring_pinky → chord_root_maj7`, `left_ring_pinky → arp_off`.

* **[2026-05-06] Phase 29 — True Modular Trigger Routing + Collision Detection:** Refactored `updateParams` from two coupled hand-blocks into three explicit phases: **Phase 1 Gather** (both hand blocks collect `activeTriggers[]` and `signals{}` independently), **Phase 2 Resolve** (iterate `triggerMappingsRef.current`, build `targetChord`/`targetArpMode`/`targetArpRate` using `dest in CHORD_DEST_LABELS` / `dest in ARP_DEST_TO_MODE` / `dest in ARP_DEST_TO_RATE` membership checks — cross-hand routing now possible), **Phase 3 Execute** (chord and arp logic uses only the resolved targets). Right-hand combo detection fully strict-enumerated (8 states, no ambiguous conditions). Left-hand combo detection fully strict-enumerated (8 states). Added **collision detection** to `GestureSettings`: frequency maps computed per-render for signal sources, signal destinations, trigger sources, and gate destinations. Selects with count > 1 receive `warningSelect` CSS class — yellow-amber border with glow. `warningSelect` uses `var(--warning-color)` for both border and text (dark amber in light mode).

* **[2026-05-06] Phase 26 — Gesture Routing Gates (Trigger Routing) UI + Audio Engine Wiring:** Extended the Gesture Settings modal with a fully editable `[ gates ]` tab that maps discrete finger-combo triggers to chord voicing and arp mode/rate actions. `src/utils/gestureMappings.js` expanded with `TRIGGER_SOURCES` (15 combo IDs across three groups: 6 right-hand chord combos, 6 left-hand arp combos, 3 left-wrist tilt bands), `TRIGGER_DESTINATIONS` (15 action IDs across three groups: 6 chord types, 6 arp modes, 3 arp rates), `DEFAULT_TRIGGER_MAPPINGS` (15 rows exactly replicating all prior hardcoded behavior), and `groupedTriggerSources()` / `groupedTriggerDestinations()` helpers that build `<optgroup>` data. `GestureSettings.jsx` gains separate trigger state handlers, id counter ref, and gates tab UI with `<select optgroup>` dropdowns and a `.rowNarrow` layout (no invert column). `App.js` wires `triggerMappings` state + `triggerMappingsRef` (inline-synced ref pattern); `useAudioEngine` now accepts `triggerMappingsRef` as its 5th parameter. In `updateParams`, a `triggerMap` (Map of trigger→destination) is built once per frame from `triggerMappingsRef.current`. Right-hand block: combo ID selected from `middleUp/ringUp/pinkyUp` state → `triggerMap.get(activeRightCombo)` → `resolveChord(dest)` → `{ voice13, voice7, thirdST, seventhST }` (replaces all `middleUp/pinkyUp` gate logic). Left-hand block: combo ID selected from `middleOut/ringOut/pinkyOut` → `triggerMap.get(activeLeftCombo)` → `ARP_DEST_TO_MODE` lookup. Tilt band selected from `tilt < 20/60` → `triggerMap.get(activeTiltBand)` → `ARP_DEST_TO_RATE` lookup. `CHORD_DEST_LABELS` drives HUD chord display. `resolveChord()` is a pure module-scope function. `getArpMode()` and `getArpRate()` helpers removed (dead code).

* **[2026-05-06] Phase 25 — Gesture Routing Modulation Matrix:** Created a user-configurable modulation matrix allowing any gesture signal to be routed to any audio parameter. Architecture: `src/utils/gestureMappings.js` defines 6 gesture sources (`right_hand_size`, `right_pinch_norm`, `left_pinch_dist`, `left_wrist_y`, `left_wrist_tilt`, `left_arp_spread` — all 0–1 normalized) and 5 audio destinations (`filter_cutoff`, `reverb_wet`, `vibrato_depth`, `volume`, `arp_volume`). `DEFAULT_MAPPINGS` replicates all prior hardcoded behavior exactly (including `left_wrist_y → vibrato_depth` with `invert: true`). `useAudioEngine` refactored: `updateParams` now has a two-phase structure — (1) extract all gesture signals into a plain `signals{}` object inside each hand block, (2) loop `mappingsRef.current` after the landmark loop and apply each mapping via `rampTo`. HUD writes for filter/reverb/vibrato/volume happen inside the mapping loop (not the hand block) so they always reflect the live routing. Complex instrument logic (pitch, chord voicing, arp mode/pattern) remains hardcoded — it IS the instrument. `mappingsRef` follows the existing `hudRefsRef`/`sendMidiRef` pattern: assigned inline in `App.js` each render, read in the rAF loop — zero React state touched in the hot path. Created `src/components/GestureSettings/GestureSettings.jsx` — fixed-position modal (z-index 200) with blurred backdrop, mint-border panel, column-labeled routing rows (source select → arrow → destination select → invert toggle → delete), footer with `[ + add routing ]` and `[ restore defaults ]`. Added "gesture routing" section to `Controls.jsx` with `[ configure routing ]` button. Refresh restores defaults (no localStorage).

* **[2026-05-03] Phase 24 — ArpTerminal Draggable Popup + FeedbackDelay + Fluid Speed:** Created `src/components/ArpTerminal/` — `ArpTerminal.jsx` and `.module.css` — a draggable overlay at `bottom: 12px; right: 56px` inside `.viewport`, mirroring the `VocoderTerminal` architecture exactly. Two DAW-style vertical faders: `dly` (5-stop delay time: 0/'1/4'/'1/8'/'1/16'/'1/32', default '1/8') and `wet` (FeedbackDelay mix 0–100%). A `[ speed snap ]` toggle below the faders switches arp rate between discrete 3-band snap (< 20° = 4n, 20–60° = 8n, 60°+ = 16n) and fluid continuous mode (wrist tilt 0–90° → 0.5–0.05s). `Tone.FeedbackDelay` inserted between `arpVol` and the bypass/fx gain split; wet=0 at init so no sound change until user raises fader. `updateParams` arp-rate logic split into two independent blocks: values/pattern delta-check (mode/root/third) and interval update. **Fluid speed architecture:** rAF loop lerps `smoothedFluidIntervalRef` (8%/frame) toward the tilt target — no scheduler contact. The `Tone.Pattern` callback reads the ref at note-fire time, sets `arpPattern.interval = fluidTime` and gates `fluidTime * 0.5` — decoupled from 60fps overload and producing a tape-warp sweep. Snap mode delta-check writes `pat.interval` from rAF unchanged (only fires on discrete tilt-band transitions). New exports: `setArpDelayTime`, `setArpDelayMix`, `setArpSpeedSnap`. `[ arp controls ]` toggle added to Controls Arpeggiator section. State wired App.js → Viewport.

* **[2026-05-02] Phase 23 — VocoderTerminal Fader Cap Redesign + Default Params:** Replaced circular thumbs with rectangular DAW-style fader caps via pure CSS on `::-webkit-slider-thumb` and `::-moz-range-thumb`. Cap is `width: 8px; height: 28px` — because the input is `rotate(-90deg)`, CSS height maps to visual width, producing a 28px-wide × 8px-tall cap. The center indicator line is a `linear-gradient(to bottom, ...)` 2px band at 50% — which, after rotation, runs horizontally through the cap's visual center. `margin-top: -13px` centers the cap on the 3px track `((3/2) - (28/2) = -12.5px)`. `.faderTrack` expanded to 28×80px for wider hit area and more drag travel. Default parameter sweet-spot locked in: `mix: 0.32`, `q: 10.0`, `envHz: 50`, `modGain: 17`, `outGain: 13` — updated in both `useVocoder.js` INIT constants and `VocoderTerminal.jsx` SLIDERS init values.

* **[2026-05-02] Phase 22 — Piano Roll Pitch Guide Lines:** Added fading horizontal guide lines extending from each white key into the camera feed as pitch-targeting reticles. CSS only — `overflow: visible` on `.pianoRoll` (was `hidden`), mask-image removed (it would clip the overflowing pseudo-elements to transparent), `position: relative` added to `.whiteKey`, `.whiteKey::after` pseudo-element with `left: 100%; width: 180px; height: 1px; background: linear-gradient(to right, rgba(93,202,165,0.4), transparent)`. 14 static guide lines (white keys only; black keys don't extend to the right edge of the roll).

* **[2026-05-02] Phase 21 — Draggable VocoderTerminal + Vertical Faders:** Added ref-based drag logic to `VocoderTerminal.jsx` — `isDragging`, `dragOffset`, and `posRef` refs enable a single `useEffect` that registers `mousemove`/`mouseup` on `window` once at mount. `handleMouseDown` on the header sets the drag origin; position is stored as `pos` state and applied via `style={{ transform: translate(x, y) }}`. Component remounts on toggle so position always resets. Converted horizontal sliders to vertical faders: each `<input type="range">` is wrapped in a `.faderTrack` div (14px × 70px) and rotated via `transform: translate(-50%,-50%) rotate(-90deg)` — the hit target rotates correctly. Layout changed to `flex-direction: row` with value/label stacked above/below each fader. Labels abbreviated ("wet", "q", "env", "mic", "out") for narrow column fit. Header gains `cursor: grab/grabbing`.

* **[2026-05-02] Phase 20 — Arp Effects Toggle + Vocoder Carrier Feed:** Added parallel gain-path routing for the arpeggiator: `arpVoice → arpVol → arpBypassGain → Destination` (bypass, default) and `arpVol → arpFxGain → filter` (effects chain). Both paths coexist — `setArpFx(bool)` cross-fades between them via `rampTo(0.05)` so the switch is click-free during playback. `[ arp thru fx ]` toggle button added to the Arpeggiator section in Controls. `currentArpFreqRef` added to `useAudioEngine`; set inside the `Tone.Pattern` callback on every fired note; included in `vocoderFreqs` array when arp mode is active, giving the vocoder a 5th carrier voice tracking the melodic arp line. `CARRIER_VOICES` bumped 4→5 in `useVocoder.js`. New nodes (`arpBypassGainRef`, `arpFxGainRef`) disposed and nulled in `stopAudio`; fx state restored on re-engage.

* **[2026-05-02] Phase 19 — VocoderTerminal Overlay:** Rewrote `useVocoder.js` to store persistent refs for all tunable nodes (`modBPFsRef`, `carrBPFsRef`, `envLPsRef`, `modPreGainRef`, `outputGainRef`). Added Dry/Wet crossfader: `micSource → dryGain → analyser`; `vocoder chain → outputGain → wetGain → analyser`; both mix at the `AnalyserNode` (fftSize 512) before `ctx.destination`. Exposed `updateVocoderParams({ q, envHz, modGain, outGain, mix })` — all AudioParam writes use `setTargetAtTime(τ=20ms)` for click-free transitions, Q/frequency updated via `.value` (instantaneous, not audio-scheduled). Exposed `getAnalyserData(Uint8Array)` wrapping `analyser.getByteFrequencyData`. Created `VocoderTerminal` component (`src/components/VocoderTerminal/`) — `position: absolute; bottom: 12px; left: 56px; z-index: 50` inside `.viewport` (which is `position: relative`), zero impact on DOM flow or camera layout. Canvas (250×60, buffer dims) runs a rAF visualizer: 40 bars, each taking the peak of adjacent FFT bins, alpha-scaled by amplitude. Five range sliders (Wet/Dry, Filter Q, Env Speed, Mic Gain, Output Gain) call `updateVocoderParams` on change via a single `handleSlider` callback. Slider values held in local React state for display only — the audio parameters are driven directly through the hook refs. `Viewport.jsx` conditionally renders the terminal when `isVocoderActive`; three new props threaded through `App.js → Viewport → VocoderTerminal`.

* **[2026-05-02] Phase 18 — Native 16-Band Vocoder:** Created `src/hooks/useVocoder.js` — a standalone Web Audio API vocoder that operates independently of Tone.js. Architecture: 16 logarithmically-spaced bands (100–8000 Hz, Q=2.5, ~35% overlap). Modulator path: `getUserMedia` mic → pre-gain (×10) → per-band BPF → full-wave rectifier (`WaveShaper`, |x| curve) → LP envelope follower (20 Hz) → drives `GainNode.gain` via AudioParam connection. Carrier path: 4 sawtooth `OscillatorNode`s (one per chord voice: root/3rd/5th/7th) → per-osc `GainNode` (equal-power mix) → carrier bus → per-band BPF → `GainNode` (controlled by envelope). Output: 16 carrier GainNodes → master output gain (×6) → `AudioContext.destination`. `updateNotes(freqs[])` uses `setTargetAtTime` (τ=10ms) for glitch-free frequency/gain updates — zero node churn in the rAF loop. `useAudioEngine` accepts `updateVocoder` as a third parameter (stored as a ref alongside `sendMidi`); `updateParams` passes active chord frequencies (`[root, third?, fifth?, seventh?]`) to the vocoder every frame. Pitch hand lost clears carrier with `updateVocoder([])`. `[ vocoder ]` toggle button added to the Output section of Controls, below MIDI, using the same `toggleBtn`/`toggleBtnActive` CSS classes. `handleToggleVocoder` async callback in `App.js` guards against double-start via `isActiveRef`.

* **[2026-05-02] Phase 17 — Smart MIDI Bridge Setup Guide:** Upgraded `MidiModal` from a single-view download prompt into a 4-view state-routed setup guide. `view` state drives four screens: `'menu'` (landing — routes to returning/windows/mac), `'returning'` (reconnect instructions + close), `'windows'` (loopMIDI + bridge download), `'mac'` (Mac/Linux bridge download). `actionsStack` CSS class handles stacked full-width ghost buttons on the menu screen; `actionsRow` handles back/primary pairs on sub-screens. Added `primaryBtn` CSS class (identical to existing `downloadBtn`) so `<a download>` links are visually indistinguishable from action buttons. Ghost back/cancel buttons use existing `closeBtn` class. VoxDaw Bridge zip bundled in `public/` and referenced via relative `/VoxDaw-MIDI-Bridge.zip` path.

* **[2026-05-02] Phase 16 — MIDI Bridge Missing Modal:** Added graceful error handling for failed WebSocket connections. `useMidi` now accepts an optional `onConnectionError` callback; `ws.onerror` calls it before closing, allowing the caller to react to a refused connection. Created `MidiModal` component (`src/components/MidiModal/MidiModal.jsx` + `.module.css`) — `position: fixed; z-index: 100` overlay with `backdrop-filter: blur(4px)`, a dark `#111114` modal box with `#5DCAA5` accent border, a heading, body copy, a primary download `<a href="/VoxDaw-MIDI-Bridge.zip" download>` button, and a ghost cancel button. Clicking the backdrop also dismisses the modal. `App.js` adds `showMidiModal` state wired to the error callback, imports and conditionally renders `<MidiModal>` after `TelemetryHUD`.

* **[2026-05-01] Phase 15 — Absolute Panel Layout + Collapsible Panels:** Switched both the Controls sidebar and TelemetryHUD from flow-positioned elements to `position: absolute` overlays. Controls: `left: 0; top: 0; height: 100vh; z-index: 10; transform: translateX(0)` → collapsed: `transform: translateX(-100%); opacity: 0` with 0.3s ease transition on explicit properties (not `transition: all`). TelemetryHUD: `position: absolute; right: 24px; top: 50%; transform: translateY(-50%)` → collapsed: `transform: translateX(120%) translateY(-50%); opacity: 0` (both transforms composed to preserve vertical centering during animation). HUD height uses `clamp(180px, calc(55vw * (9/16)), 450px)` to track camera aspect-ratio height. `.app` changed to `position: relative; display: flex; justify-content: center; align-items: center` so the camera stage (`div.stage`) is permanently centered in the viewport regardless of panel state. `showControls` / `showAnalytics` boolean state added to `App.js`. Two independent `position: absolute; z-index: 50` toggle buttons (`leftToggleBtn` / `rightToggleBtn`) placed directly in `App.js` (not inside the panels) so they remain visible and clickable through panel collapse animations. Viewport resized to `width: 65vw; max-width: 1000px; aspect-ratio: 16/9`.

* **[2026-05-01] Phase 14 — Typography & Visual Hierarchy Polish:** TelemetryHUD metric labels: `font-size` 7px → 9px, opacity 0.45 → 0.72, letter-spacing adjusted for legibility. Engage button upgraded to a distinct primary CTA: `background: rgba(93,202,165,0.15); border: 2px solid #5DCAA5`; hover floods with 82% opacity accent green and flips text to `#111114`. Controls section titles gained a hairline `border-bottom: 1px solid rgba(255,255,255,0.08)` + `padding-bottom: 4px` to headline each group; opacity increased to 0.55.

* **[2026-05-01] Phase 13 — Studio Cockpit Polish + Engage Button Relocation:** Sidebar shrunk 280px → 200px (140px rejected as too narrow for existing label text). Engage/disengage button extracted from the Controls sidebar and placed below the camera feed in the main stage, centered relative to the Viewport only. `mainStage` changed to `flex-direction: column; align-items: center`. MIDI toggle, record button, and all sidebar controls unaffected. CSS mini density pass: section padding `12px 16px`, gap `10px`, button padding `6px 12px`, engage area padding `14px`, octave buttons `28×26px`.

* **[2026-05-01] Phase 12 — Dynamic MIDI Velocity + Octave Controls:** Chord MIDI velocity derived from pinch-normalized distance (`mapRange(normalizedPinch, 0.2, 1.2, 1, 127)`); arp MIDI velocity derived from `spreadDb` (`mapRange(spreadDb, -12, 3, 1, 127)`). Global octave shift (+/- buttons, range −2 to +2) wired through `setGlobalOctave` → `globalOctaveRef`; raw pitch computation now uses `Tone.Frequency(note).transpose(globalOctaveRef.current * 12)`. Arp +1 oct toggle (`arpOctaveShiftRef`) adds 12 semitones to `effectiveArpRoot` before the arp delta-check. Both controls exposed in Controls sidebar and threaded through App.js. `appliedArpThirdRef` added to the arp delta-check set so chord quality changes also trigger pattern rebuilds.

* **[2026-05-01] Phase 12 — Left Control Panel Sidebar:** Refactored the entire Controls layout from a horizontal bottom bar into a 280px vertical DAW-style sidebar. `App.css` changed to `flex-direction: row`; `Controls` now renders before `mainStage` in JSX so it sits on the left. `Controls.jsx` reorganized into four labeled sections: **Master Engine** (tempo, octave, scale), **Synth Voice** (instrument, conditional oscillator), **Arpeggiator** (arp +1 oct toggle), **Output** (MIDI toggle, conditional record). The engage/disengage button lives in a dedicated `.engageArea` with `margin-top: auto` so it pins to the bottom regardless of content height. `Controls.module.css` fully rewritten: sidebar uses `flex-direction: column` at `#111114` background, `.section` blocks have `border-bottom` dividers, all inputs given `width: 100%` for full-bleed layout, MIDI/arp toggles use new `.toggleBtn`/`.toggleBtnActive` classes with subtle active-state background fill, all button text is `text-align: left` for DAW-panel feel.

* **[2026-05-01] Phase 11 — Live MIDI WebSocket Output:** Added `src/hooks/useMidi.js` — a dedicated hook that owns the WebSocket lifecycle (`ws://localhost:8080`), MIDI enabled state, and a panic flush. `sendMidi(type, note, velocity)` gates on a `midiEnabledRef` shadow (safe inside rAF/Tone callbacks) and `WebSocket.OPEN`, and maintains an `activeMidiNotes` Set for the panic. `toggleMidi` turns on by opening a new WS and setting `isMidiEnabled` only inside `ws.onopen`; turns off by calling `panicAllNotes` first then closing — connection failures revert state automatically via `ws.onerror → ws.close`. `useAudioEngine` gains a `sendMidi` second parameter (stored as `sendMidiRef` via the existing hudRefs pattern) plus `chordMidiRef` (Set for per-frame MIDI diff) and `arpMidiTimers` (array of noteoff setTimeout IDs). The right-hand block diffs the current chord MIDI note set against the previous frame and sends noteon/noteoff only for changed notes; pitch-hand-lost fires noteoffs for all active chord notes. The arp Pattern callback sends `noteon` immediately and schedules `noteoff` via `setTimeout` using `Tone.Transport.toSeconds(holdInterval) * 1000`. `stopAudio` cancels all pending arp noteoff timers and sends noteoffs for any remaining chord notes. `App.js` calls `panicAllNotes()` at the top of `handleDisengage` before stopping audio. Controls gains a `[ midi ]` / `● midi` toggle button in the button group, styled with `.midiBtn` / `.midiBtnActive` CSS classes matching the existing premium aesthetic.

* **[2026-05-01] Phase 10 — Master Tempo (BPM) Control:** Added a `bpmRef` (default 120) to `useAudioEngine` and a `setTempo(bpm)` callback that calls `Tone.Transport.bpm.rampTo(bpm, 0.1)` for glitch-free real-time tempo changes. `startAudio` now seeds the Transport from `bpmRef.current` instead of a hardcoded 120, so any pre-engage BPM setting is honoured and the value persists across disengage/re-engage cycles. Added a styled `type="number"` input (label: "tempo", range 40–300, default 120) to `Controls.jsx` inside the existing `selectGroup`, with a `.bpmInput` CSS rule that mirrors the `.select` aesthetic (same border, colour, font, radius) at a compact 72px width. Wired through `App.js` via `onTempoChange={setTempo}` prop.

* **[2026-05-01] Phase 9 — Arp Key Flash + Pattern Stutter Fix:** Added gold visual feedback to the Piano Roll that fires on each arpeggiator step. `PianoRoll` gains a second imperative method `flashNote(noteName, durationMs)` — adds a `.flash` CSS class driving an inset `box-shadow` animation in `rgba(255,209,102,0.92)` (neon gold) that fades over 180ms, layered on top of the chord `.active` state without background conflicts. `flashTimersRef` (Map) cancels in-flight timers and forces a reflow (`void el.offsetWidth`) so rapid re-fires on the same key always restart the animation cleanly. Called directly from the `Tone.Pattern` callback (Pattern runs in the main thread; `Tone.Draw` was dropped after analysis showed its per-event rAF loop could silently expire callbacks under load). Fixed a 60FPS stutter bug: `pat.values`, `pat.pattern`, and `pat.interval` were overwritten every frame, restarting Tone.Pattern's step counter before it could advance. Three new delta-check refs (`appliedArpModeRef`, `appliedArpRateRef`, `appliedArpRootRef`) gate pattern writes to only fire on actual changes. All three are reset to `null` in `stopAudio` to prevent stale values from skipping updates on re-engage.

* **[2026-04-30] Phase 9 — Gesture-Controlled Arpeggiator (Right Hand):** Added a `Tone.Pattern`-based arpeggiator driven entirely by right-hand gestures. Three new pure functions added to `dsp.js`: `getArpFingerStates(hand)` (rotation-robust wrist-to-tip vs wrist-to-PIP distance ratio, 1.1× threshold for noise immunity), `getWristTiltDeg(hand)` (atan2 of wrist→midMCP vector normalized to upright=0°, absolute deviation for bidirectional tilt), and `getArpSpreadDb(hand, states)` (outermost extended-tip Euclidean distance → −24 to 0 dB). `useAudioEngine` gains a dedicated `Tone.Synth` arp voice (triangle, plucky 5ms attack / 120ms decay) plus a dedicated `Tone.Volume` tapped before the shared filter so arp gain is independent of the main pinch-velocity volume. `Tone.Pattern` is created once in `startAudio`, always running on `Tone.Transport` (120 BPM), and silenced via `pat.mute` — no start/stop churn on each rAF frame. Six modes mapped to finger combinations: OFF / SIMPLE (upDown, 1-oct major) / COMPLEX (upDown, 2-oct) / CMPLX RND (randomWalk, 2-oct) / SMP RND (random, 1-oct) / OCTAVE (root+12, upDown). Rate snaps in three bands: < 20° = 4n, 20–60° = 8n, 60°+ = 16n (absolute wrist tilt from vertical). When arp is active, all four chord voices are muted via `rampTo(VOICE_MUTE)`; chord logic restores automatically on transition back to OFF. `Tone.Transport` started in `startAudio` and stopped in `stopAudio`. `arpRef` added to `hudRefs` and `TelemetryHUD` displays current mode label.

* **[2026-04-29] Phase 8 — Piano Roll HUD:** Added a vertical 2-octave (C3–B4) piano keyboard to the left edge of the viewport that lights up in real time to reflect the active chord. `PianoRoll` is a `forwardRef` component exposing a single imperative `setNotes(noteNames[])` method via `useImperativeHandle` — zero React state, zero re-renders. Key layout computed once at module scope: 14 white keys (B4→C3) as a flex column, 10 black keys as an absolute overlay with `topPct = ((whiteIndexAbove) + 0.7) / 14 × 100` and height `0.6/14 × 100 ≈ 4.29%`. Internal `keyMapRef` (Map) enables O(1) element lookup; `activeSetRef` (Set) enables diff-based class toggling so frames with no chord change produce zero DOM writes. `normalizeNote()` added to `dsp.js` maps Tone.js flat names (Bb/Eb/Ab/Db/Gb) to sharps (A#/D#/G#/C#/F#). `updateParams` computes `activeNoteNames` using `Tone.Frequency(hz).toNote()` after chord gate logic and calls `hud.pianoRoll.current.setNotes()`; a `pitchHandFound` flag clears the keyboard when the pitch hand leaves frame. `stopAudio` clears the keyboard before disposal. Piano styled as `position:absolute, left:0, width:44px`, with a `mask-image` gradient fade on the right edge to integrate with the camera feed. Active keys: `#5DCAA5` (full on black, 0.82 opacity on white) with 40ms CSS transition.

* **[2026-04-29] Phase 7 — The Instrument Rack:** Added a second parallel voice set — 4× `Tone.FMSynth` strings voices — alongside the existing 4× `Tone.Synth` analog voices. Both sets connect to the shared Filter → Vibrato → Reverb → Volume effects chain; Tone.js sums them at the filter input. Strings preset: `harmonicity: 3.5`, `modulationIndex: 10`, sawtooth carrier, sine modulator, `envelope.attack: 0.3s` / `release: 0.8s`, `modulationEnvelope.attack: 0.5s` (delayed modulation bloom mimics bowing). Active set tracked via `activeVoicesRef` (plain ref, zero re-renders); `currentRootRef` stores last-known root so `setInstrument` can snap incoming voices to the correct frequency. Switching: old set fades to VOICE_MUTE over 100ms, new set root fades to VOICE_DB — chord harmonics follow on next rAF frame. `stopAudio` dispose timeout extended to 1200ms for strings 0.8s release. `setOscType` restricted to analog voices only. Added instrument `<select>` as the first dropdown in `Controls.jsx`; oscillator dropdown conditionally hidden when strings is active. `INSTRUMENTS` constant defined inline in Controls. `setInstrument` exported from `useAudioEngine`; wired through `App.js` → `onInstrumentChange` prop.

* **[2026-04-29] Phase 6 — Polyphony & Gesture Chords:** Replaced single `Tone.Synth` with 4 explicit synth voices (`synthsRef: Tone.Synth[]`) — voices 0–3 map to root, 3rd, 5th, and minor 7th respectively. All 4 voices connect directly into the shared filter input (Tone.js sums them automatically); no mixer node needed. Each voice initializes at `volume.value = -12dB` to compensate for 4× summation. Added `isFingerExtended(hand, tipIndex, pipIndex)` to `dsp.js` — compares tip Y to PIP Y (PIP chosen over MCP for reliable curl detection on angled wrists). Voice 1 (3rd): middle finger extended (tip 12 vs PIP 10) → Major 3rd (×1.2599), closed → Minor 3rd (×1.1892). Voice 2 (5th): always active at root × 1.4983. Voice 3 (7th): pinky extended (tip 20 vs PIP 18) → `volume.rampTo(-12, 0.05)` active; closed → `volume.rampTo(-80, 0.05)` muted — smooth gate with no click artifacts. Chord quality label computed per-frame and written to new `chordRef` DOM ref (MAJOR / MINOR / MAJ 7 / MIN 7). Added `chord` metric to `TelemetryHUD` between pitch and filter rows. `setOscType` updated to iterate all 4 voices; `oscTypeRef` caches current type for re-engage. Zero new React state, zero new re-renders.

* **[2026-04-29] Phase 4 — Loop Station:** Added `useLoopStation` hook (`src/hooks/useLoopStation.js`) — owns `Tone.Recorder` (connected to `volumeRef` post-engage), `Tone.Player` (loop:true), and a `progressRef` for zero-re-render progress bar animation. Recording runs for a fixed 8s (4 bars @ 120 BPM) gated by `setTimeout`; `requestAnimationFrame` drives `scaleX(0→1)` on the progress bar element. After recording, blob is decoded via `Tone.getContext().rawContext.decodeAudioData(arrayBuffer)` → `Tone.ToneAudioBuffer` → `Tone.Player.start()`. Exported `volumeRef` from `useAudioEngine`. Created `LoopProgress` component (`src/components/LoopProgress/`) — `position: absolute` at top of Viewport, red while recording, accent-green while looping, 16 tick marks. Added `[ record ]` / `● rec` / `[ re-record ]` button to Controls bar with pulse-red `@keyframes` animation on active state. `dispose()` called on disengage to clean up recorder and player. Live synth continues unaffected during and after loop playback (overdub).

* **[2026-04-29] Velocity Normalization Fix:** Replaced raw thumb-index `calculateDistance` (3D) with `calculateDistance2D(thumbTip, indexTip) / handSize` — dividing by the wrist-to-MCP9 reference distance cancels the perspective scaling so the ratio stays constant regardless of how far the hand is from the camera. Updated volume mapping to `mapRange(normalizedPinch, 0.2, 1.2, -40, 0)` and HUD velocity display to match. Left hand's reverb `dist` mapping unchanged (that parameter benefits from absolute distance as a natural "how far apart are the fingers" signal).
* **[2026-04-29] Project Documentation Initialized:** Created foundational `claude.md`, `architecture.md`, and `PLAN.md` files to establish system instructions, architecture, and task tracking for the AI agent.
* **[2026-04-29] Phase 1 UI Scaffolding:** Built the initial dark premium layout shell using CSS Modules and Option A palette (`#0e0e10` / `#5DCAA5`). Created three modular components — `Viewport` (16:9 camera placeholder with accent glow border), `TelemetryHUD` (static `--` readouts for Pitch, Reverb, Velocity with prop stubs ready for Phase 4 wiring), and `Controls` (bottom bar with `[ engage ]` button). Stripped all CRA default boilerplate. Verified clean compile with `npm start`.
* **[2026-04-29] Phase 4 — Telemetry HUD Expansion (Filter + Vibrato):** Added `filterRef` and `vibratoRef` to the `hudRefs` object in `App.js`. Expanded `TelemetryHUD.jsx` from 3 to 5 metrics (Pitch, Filter, Reverb, Vibrato, Velocity) using a metrics array — no new CSS needed, identical `.metricLabel`/`.metricValue` classes. Filter Hz written from the right-hand block (`cutoff` was already computed); Vibrato % written from the left-hand block (`depth` was already computed). Both DOM writes land inside the existing rAF `updateParams` call — zero additional overhead, zero React re-renders.
* **[2026-04-29] Phase 4 — Quantization, Z-Axis Filter, Control Panel:** Created `src/utils/scales.js` — pre-computes sorted `Float32Array` frequency tables at module load for C Major, C Minor Pentatonic, and Chromatic scales (MIDI 0–127 iterated monotonically, so output is pre-sorted with no `.sort()` call). Added `snapToNearest(hz, sortedFreqs)` to `dsp.js` — O(log n) binary search using unsigned right-shift mid-point, snaps raw pitch to nearest scale frequency each rAF frame. Added `Tone.Filter` (low-pass, −24dB/oct rolloff) to front of signal chain (`Synth → Filter → Vibrato → Reverb → Volume → Destination`). Right wrist Z-axis mapped to filter cutoff (wrist toward camera → 10000Hz open; wrist away → 400Hz dark). Exposed `setOscType(type)` and `setScale(key)` from `useAudioEngine` — both stable `useCallback` functions writing directly to refs, zero re-renders. Rebuilt `Controls.jsx` with two `<select>` dropdowns (Scale + Oscillator) maintaining local state and calling parent callbacks on change; layout is `space-between` with the dropdown cluster left and engage button right. All browser default select chrome removed (`appearance: none`); custom accent-colour chevron arrow via SVG data URI; options styled `background: #16161c`.
* **[2026-04-29] Phase 3 — Dual-Hand DSP Effects Chain + Telemetry HUD:** Expanded `useAudioEngine.js` with `Tone.Vibrato` (frequency: 5Hz, depth: 0–0.8) and `Tone.Reverb` (decay: 3s, wet: 0–1). Signal chain: `Synth → Vibrato → Reverb → Volume → Destination`. Reverb IR pre-baked with `await reverb.generate()` before `triggerAttack` so the first note never hits an uninitialized buffer. Dual-hand routing via `result.handednesses` from MediaPipe — accounts for CSS mirror inversion (MediaPipe `"Left"` = user's right hand). Right hand: pitch (wrist Y → 200–800Hz) + velocity (thumb-index dist → −40–0dB). Left hand: reverb wet (thumb-index dist → 0–100%) + vibrato depth (wrist Y → 0–0.8). Telemetry HUD rewritten to accept DOM refs (`pitchRef`, `reverbRef`, `velocityRef`) instead of state props — values written via `element.textContent` directly inside the rAF loop. Zero React re-renders at runtime; `hudRefs` object synced into a ref inside the hook on each render to keep `updateParams` dependency-free.
* **[2026-04-29] Phase 3 — Tone.js Audio Engine & Gesture Mapping:** Installed `tone@15.1.22`. Created `src/utils/dsp.js` with pure `calculateDistance(p1, p2)` (3D Euclidean) and `mapRange(value, inMin, inMax, outMin, outMax)` (clamped linear interpolation). Created `src/hooks/useAudioEngine.js` — owns full Tone.js lifecycle: `Tone.Synth` (sine, sustain envelope) connected through a `Tone.Volume` node to `Tone.Destination`. `startAudio()` calls `await Tone.start()` (satisfies browser autoplay policy) then `triggerAttack(440)`. `stopAudio()` calls `triggerRelease()` then disposes both nodes after 600ms (ensures 500ms release envelope fully decays before disposal). `updateParams(landmarks)` runs inside the existing rAF loop — maps wrist Y (landmark 0) → 200–800Hz via `frequency.rampTo(hz, 0.05)`, and thumb-to-index distance (landmarks 4 & 8) → −40–0dB via `volume.rampTo(db, 0.05)`. 50ms ramp time chosen to prevent zipper noise while maintaining sub-frame-latency gesture response. `onLandmarks` callback stored in a ref inside `useHandTracking` to prevent rAF loop restarts on callback identity changes. `App.js` sequences `startAudio → engage` on click and `disengage → stopAudio` on release.
* **[2026-04-29] Phase 2 — MediaPipe HandLandmarker + rAF Skeleton:** Installed `@mediapipe/tasks-vision@0.10.35`. Created `src/hooks/useHandTracking.js` — initializes `HandLandmarker` once on mount (async, non-blocking, `runningMode: "VIDEO"`), starts a `requestAnimationFrame` loop gated on `isActive && isReady`, and draws 21-point hand skeleton in `#5DCAA5` (connections at 0.75 alpha, landmark dots at full opacity) onto an absolutely-positioned `<canvas>` overlay in `Viewport`. Loop polls `video.readyState` before each `detectForVideo()` call. `performance.now()` used for timestamps (monotonic, sub-ms precision). Canvas dimensions synced to `videoWidth/videoHeight` natively; CSS scales to fill container. Cleanup cancels rAF and clears canvas on disengage/unmount. `GENERATE_SOURCEMAP=false` in `.env` suppresses a known upstream webpack source-map warning from the MediaPipe package.
* **[2026-04-29] Phase 2 — getUserMedia Camera Toggle:** Created `src/hooks/useCameraStream.js` — a custom hook encapsulating `getUserMedia`, track cleanup, and error state. Stream assigned via `video.srcObject` (zero-copy, no blob URL). `Viewport` now accepts `videoRef/isActive/error` props and renders live `<video>` (object-fit: cover) or an error label (`#E07070`). `Controls` button toggles `[ engage ]` ↔ `[ disengage ]` with distinct crimson styling for the active/destructive state. Unmount `useEffect` cleanup prevents dangling media tracks. Verified clean build with `react-scripts build`.
* **[2026-04-29] Phase 1 Dark Premium Styling & Contrast Pass:** Increased opacity of all accent-colored elements to full legibility (`#5DCAA5` fully opaque on button/values, 0.75 on viewport border). Added a frosted-glass depth panel behind the Telemetry HUD (`rgba(16,16,22,0.85)` background + `border: #2a2a2e` + `backdrop-filter: blur`). Applied a subtle `linear-gradient(#16161c → #0e0e10)` to the Controls strip to lift it off the void. Raised muted label opacity from 0.35 → 0.6.